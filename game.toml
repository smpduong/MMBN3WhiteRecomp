# Mega Man Battle Network 3 White (USA, A6BE, revision 0).
# Initial bring-up configuration consumed by both gba_recompile and the runtime.
# Pattern follows mstan/MegaManZeroRecomp/game.toml; misses get appended as
# reviewed [[extra_func]] / [[code_copy]] entries, never hand-edited generated code.

[game]
name = "Mega Man Battle Network 3 White"
short_name = "mmbn3_white"

[program]
name = "Mega Man Battle Network 3 White (USA)"
id = "A6BE"
load_address = 0x08000000
size = 0x00800000
entry_pc = 0x08000000
# Strict-static corpus policy: callbacks admitted from observed LLE
# dispatch targets, not speculative literal words.
speculative_literal_harvest = false
codegen_shards = 16

[identity]
sha1 = "ff45038ae6d01cde4eae25a02dcb8bed29e07a6f"
md5 = "68817204a691449e655cba739dbb0165"

[bios]
path = "../gbarecomp/bios/gba_bios.bin"
sha1 = "300c20df6731a33952ded8c436f7f186d25d3492"
hle = false
# R3 experiment: full BIOS boot (no skip) so the recompiled BIOS installs its
# IRQ handler before cart entry. Skip boots left zeroed IWRAM + IRQ depth spin.
skip_intro = false

[rom]
path = "roms/mmbn3_white_usa.gba"
sha1 = "ff45038ae6d01cde4eae25a02dcb8bed29e07a6f"

[save]
type = "sram"
size = 32768
path = "saves/mmbn3_white_usa.sav"

[runtime]
debug_port = 19863
window_title = "MMBN3WhiteRecomp"

[video]
# Raw passthrough (was "frontlit": its panel simulation lifts blacks to grey
# across the whole game — reported live 2026-09-06).
screen = "raw"
view_width = 240
resize_view = false

[audio]
shadow = false

# Round 1 (2026-09-04): reviewed from recomp_master_misses_A6BE.toml.frag.
# All lie outside the 473-function finder corpus and were bridged x>=1 live.
# The 0x08001CBA..0x08001CD2 run is flagged a jump-table candidate; admitted
# as individual roots for now, to be resized into one [[jump_table]] once the
# dispatcher base is identified. BIOS 0x00000018/0x00000128 are engine-owned
# (see gbarecomp/bios/gba_bios.toml), not duplicated here.

[[extra_func]]
addr = 0x08001CBA
mode = "thumb"
note = "R1: jump-table candidate case target, bridged x1"

[[extra_func]]
addr = 0x08001CC2
mode = "thumb"
note = "R1: jump-table candidate case target, bridged x1"

[[extra_func]]
addr = 0x08001CCA
mode = "thumb"
note = "R1: jump-table candidate case target, bridged x1"

[[extra_func]]
addr = 0x08001CD2
mode = "thumb"
note = "R1: jump-table candidate case target, bridged x1"

[[extra_func]]
addr = 0x08001F24
mode = "thumb"
note = "R1: executed indirect/fall-through target, bridged x1"

[[extra_func]]
addr = 0x0800529C
mode = "thumb"
note = "R1: executed indirect/fall-through target, bridged x1"

[[extra_func]]
addr = 0x0802B344
mode = "thumb"
note = "R1: executed indirect/fall-through target, bridged x1"

[[extra_func]]
addr = 0x0802B3CA
mode = "thumb"
note = "R1: executed indirect/fall-through target, bridged x1"

[[extra_func]]
addr = 0x0802B3E2
mode = "thumb"
note = "R1: executed indirect/fall-through target, bridged x1"

[[extra_func]]
addr = 0x08132CE0
mode = "thumb"
note = "R1: hottest miss, bridged x29"

# Round 2 (2026-09-04): 30s verbose boot. R1 roots covered (no repeats).
# New ROM frontier: Thumb-mode entry into ARM function gf_afunc_0802B354
# (interworking) and interior resume inside gf_tfunc_08001E70.
# IWRAM misses (0x03005E00 arm, 0x03005FD0/0x030068BA thumb) are copied
# runtime code — need [[code_copy]] with ROM source backing (capture via
# oracle before admitting; NOT added as bare extra_func).

[[extra_func]]
addr = 0x0802B354
mode = "thumb"
note = "R2: Thumb interwork entry into ARM function, bridged live"

[[extra_func]]
addr = 0x08001E8E
mode = "thumb"
resume = true
note = "R2: interior resume inside gf_tfunc_08001E70, bridged live"

# Round 3: <ROM-BOOT> frontier, 228 entries (13 unsized jump-table regions admitted as roots; size later). IWRAM entries decode via iwram_sound_mixer code_copy.

[[code_copy]]
runtime_start = 0x03005E00
source_start = 0x08235F00
size = 0x000017AC
name = "iwram_sound_mixer"
note = "R3: maximal exact live-IWRAM/ROM run; mixer reachable via 0x03005E00/5FD0/6824/68BA misses"

[[extra_func]]
addr = 0x03005E00
mode = "arm"
note = "R3: proposed from self-heal miss-log; bridged x685089"

[[extra_func]]
addr = 0x03005FD0
mode = "thumb"
note = "R3: proposed from self-heal miss-log; bridged x4"

[[extra_func]]
addr = 0x03006824
mode = "thumb"
note = "R3: proposed from self-heal miss-log; bridged x3030"

[[extra_func]]
addr = 0x030068BA
mode = "thumb"
note = "R3: proposed from self-heal miss-log; bridged x13008"

# ── R3 JUMP-TABLE (unsized, admitted as roots): 6 consecutive thumb misses in [0x08000304, 0x0800031C] ──
# Likely the case targets of a computed-jump switch the finder
# could not size. PREFER one sized [[jump_table]] over the 6
# [[extra_func]] below: find the abs32 table base (the
# `ldr rT,[pc,#..]; add rT,index<<2; ldr/mov pc` dispatcher) and
# add `[[jump_table]] addr=<base> stride=4 count=<CMP bound> format="abs32" entries_mode="auto"`.

[[extra_func]]
addr = 0x08000304
mode = "thumb"
note = "R3: proposed from self-heal miss-log; bridged x70; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08000308
mode = "thumb"
note = "R3: proposed from self-heal miss-log; bridged x101; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08000310
mode = "thumb"
note = "R3: proposed from self-heal miss-log; bridged x381; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08000314
mode = "thumb"
note = "R3: proposed from self-heal miss-log; bridged x147; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08000318
mode = "thumb"
note = "R3: proposed from self-heal miss-log; bridged x85; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x0800031C
mode = "thumb"
note = "R3: proposed from self-heal miss-log; bridged x137; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x080005E2
mode = "thumb"
note = "R3: proposed from self-heal miss-log; bridged x1"

[[extra_func]]
addr = 0x0800067C
mode = "thumb"
note = "R3: proposed from self-heal miss-log; bridged x1"

[[extra_func]]
addr = 0x08001E84
mode = "thumb"
note = "R3: proposed from self-heal miss-log; bridged x125"

# ── R3 JUMP-TABLE (unsized, admitted as roots): 4 consecutive thumb misses in [0x0800200C, 0x0800206C] ──
# Likely the case targets of a computed-jump switch the finder
# could not size. PREFER one sized [[jump_table]] over the 4
# [[extra_func]] below: find the abs32 table base (the
# `ldr rT,[pc,#..]; add rT,index<<2; ldr/mov pc` dispatcher) and
# add `[[jump_table]] addr=<base> stride=4 count=<CMP bound> format="abs32" entries_mode="auto"`.

[[extra_func]]
addr = 0x0800200C
mode = "thumb"
note = "R3: proposed from self-heal miss-log; bridged x121; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x0800201C
mode = "thumb"
note = "R3: proposed from self-heal miss-log; bridged x598; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08002022
mode = "thumb"
note = "R3: proposed from self-heal miss-log; bridged x193; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x0800206C
mode = "thumb"
note = "R3: proposed from self-heal miss-log; bridged x130; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

# ── R3 JUMP-TABLE (unsized, admitted as roots): 3 consecutive thumb misses in [0x08005288, 0x080052F2] ──
# Likely the case targets of a computed-jump switch the finder
# could not size. PREFER one sized [[jump_table]] over the 3
# [[extra_func]] below: find the abs32 table base (the
# `ldr rT,[pc,#..]; add rT,index<<2; ldr/mov pc` dispatcher) and
# add `[[jump_table]] addr=<base> stride=4 count=<CMP bound> format="abs32" entries_mode="auto"`.

[[extra_func]]
addr = 0x08005288
mode = "thumb"
note = "R3: proposed from self-heal miss-log; bridged x2; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x080052D4
mode = "thumb"
note = "R3: proposed from self-heal miss-log; bridged x366; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x080052F2
mode = "thumb"
note = "R3: proposed from self-heal miss-log; bridged x1; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

# ── R3 JUMP-TABLE (unsized, admitted as roots): 3 consecutive thumb misses in [0x08005378, 0x080053D4] ──
# Likely the case targets of a computed-jump switch the finder
# could not size. PREFER one sized [[jump_table]] over the 3
# [[extra_func]] below: find the abs32 table base (the
# `ldr rT,[pc,#..]; add rT,index<<2; ldr/mov pc` dispatcher) and
# add `[[jump_table]] addr=<base> stride=4 count=<CMP bound> format="abs32" entries_mode="auto"`.

[[extra_func]]
addr = 0x08005378
mode = "thumb"
note = "R3: proposed from self-heal miss-log; bridged x15; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x080053A4
mode = "thumb"
note = "R3: proposed from self-heal miss-log; bridged x50; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x080053D4
mode = "thumb"
note = "R3: proposed from self-heal miss-log; bridged x1; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

# ── R3 JUMP-TABLE (unsized, admitted as roots): 3 consecutive thumb misses in [0x08005460, 0x0800547C] ──
# Likely the case targets of a computed-jump switch the finder
# could not size. PREFER one sized [[jump_table]] over the 3
# [[extra_func]] below: find the abs32 table base (the
# `ldr rT,[pc,#..]; add rT,index<<2; ldr/mov pc` dispatcher) and
# add `[[jump_table]] addr=<base> stride=4 count=<CMP bound> format="abs32" entries_mode="auto"`.

[[extra_func]]
addr = 0x08005460
mode = "thumb"
note = "R3: proposed from self-heal miss-log; bridged x18; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x0800547A
mode = "thumb"
note = "R3: proposed from self-heal miss-log; bridged x15; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x0800547C
mode = "thumb"
note = "R3: proposed from self-heal miss-log; bridged x4; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x080071C2
mode = "thumb"
note = "R3: proposed from self-heal miss-log; bridged x101"

[[extra_func]]
addr = 0x080071EA
mode = "thumb"
note = "R3: proposed from self-heal miss-log; bridged x349"

# ── R3 JUMP-TABLE (unsized, admitted as roots): 30 consecutive thumb misses in [0x08021F9C, 0x080221B4] ──
# Likely the case targets of a computed-jump switch the finder
# could not size. PREFER one sized [[jump_table]] over the 30
# [[extra_func]] below: find the abs32 table base (the
# `ldr rT,[pc,#..]; add rT,index<<2; ldr/mov pc` dispatcher) and
# add `[[jump_table]] addr=<base> stride=4 count=<CMP bound> format="abs32" entries_mode="auto"`.

[[extra_func]]
addr = 0x08021F9C
mode = "thumb"
note = "R3: proposed from self-heal miss-log; bridged x183; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08021FAC
mode = "thumb"
note = "R3: proposed from self-heal miss-log; bridged x486; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08021FB0
mode = "thumb"
note = "R3: proposed from self-heal miss-log; bridged x366; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08021FC4
mode = "thumb"
note = "R3: proposed from self-heal miss-log; bridged x1; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08021FEA
mode = "thumb"
note = "R3: proposed from self-heal miss-log; bridged x1; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08021FF2
mode = "thumb"
note = "R3: proposed from self-heal miss-log; bridged x1; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08021FF6
mode = "thumb"
note = "R3: proposed from self-heal miss-log; bridged x1; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08022030
mode = "thumb"
note = "R3: proposed from self-heal miss-log; bridged x486; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x0802203C
mode = "thumb"
note = "R3: proposed from self-heal miss-log; bridged x192; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x0802204A
mode = "thumb"
note = "R3: proposed from self-heal miss-log; bridged x142; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x080220A8
mode = "thumb"
note = "R3: proposed from self-heal miss-log; bridged x1; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x080220D0
mode = "thumb"
note = "R3: proposed from self-heal miss-log; bridged x1; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x080220D4
mode = "thumb"
note = "R3: proposed from self-heal miss-log; bridged x1; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x080220E4
mode = "thumb"
note = "R3: proposed from self-heal miss-log; bridged x173; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x080220FC
mode = "thumb"
note = "R3: proposed from self-heal miss-log; bridged x1; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x080220FE
mode = "thumb"
note = "R3: proposed from self-heal miss-log; bridged x130; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08022104
mode = "thumb"
note = "R3: proposed from self-heal miss-log; bridged x1; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08022110
mode = "thumb"
note = "R3: proposed from self-heal miss-log; bridged x1; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08022112
mode = "thumb"
note = "R3: proposed from self-heal miss-log; bridged x111; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x0802211E
mode = "thumb"
note = "R3: proposed from self-heal miss-log; bridged x115; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08022122
mode = "thumb"
note = "R3: proposed from self-heal miss-log; bridged x134; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08022138
mode = "thumb"
note = "R3: proposed from self-heal miss-log; bridged x16; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08022148
mode = "thumb"
note = "R3: proposed from self-heal miss-log; bridged x16; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08022166
mode = "thumb"
note = "R3: proposed from self-heal miss-log; bridged x1; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08022168
mode = "thumb"
note = "R3: proposed from self-heal miss-log; bridged x119; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08022184
mode = "thumb"
note = "R3: proposed from self-heal miss-log; bridged x116; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08022190
mode = "thumb"
note = "R3: proposed from self-heal miss-log; bridged x103; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x0802219C
mode = "thumb"
note = "R3: proposed from self-heal miss-log; bridged x99; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x080221A8
mode = "thumb"
note = "R3: proposed from self-heal miss-log; bridged x103; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x080221B4
mode = "thumb"
note = "R3: proposed from self-heal miss-log; bridged x35; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08022260
mode = "thumb"
note = "R3: proposed from self-heal miss-log; bridged x100"

[[extra_func]]
addr = 0x08022284
mode = "thumb"
note = "R3: proposed from self-heal miss-log; bridged x118"

# ── R3 JUMP-TABLE (unsized, admitted as roots): 9 consecutive thumb misses in [0x080224AC, 0x08022522] ──
# Likely the case targets of a computed-jump switch the finder
# could not size. PREFER one sized [[jump_table]] over the 9
# [[extra_func]] below: find the abs32 table base (the
# `ldr rT,[pc,#..]; add rT,index<<2; ldr/mov pc` dispatcher) and
# add `[[jump_table]] addr=<base> stride=4 count=<CMP bound> format="abs32" entries_mode="auto"`.

[[extra_func]]
addr = 0x080224AC
mode = "thumb"
note = "R3: proposed from self-heal miss-log; bridged x127; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x080224C0
mode = "thumb"
note = "R3: proposed from self-heal miss-log; bridged x95; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x080224CE
mode = "thumb"
note = "R3: proposed from self-heal miss-log; bridged x168; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x080224DC
mode = "thumb"
note = "R3: proposed from self-heal miss-log; bridged x131; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x080224EA
mode = "thumb"
note = "R3: proposed from self-heal miss-log; bridged x127; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x080224F8
mode = "thumb"
note = "R3: proposed from self-heal miss-log; bridged x115; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08022506
mode = "thumb"
note = "R3: proposed from self-heal miss-log; bridged x83; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08022514
mode = "thumb"
note = "R3: proposed from self-heal miss-log; bridged x113; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08022522
mode = "thumb"
note = "R3: proposed from self-heal miss-log; bridged x156; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

# ── R3 JUMP-TABLE (unsized, admitted as roots): 14 consecutive thumb misses in [0x080225B6, 0x0802264C] ──
# Likely the case targets of a computed-jump switch the finder
# could not size. PREFER one sized [[jump_table]] over the 14
# [[extra_func]] below: find the abs32 table base (the
# `ldr rT,[pc,#..]; add rT,index<<2; ldr/mov pc` dispatcher) and
# add `[[jump_table]] addr=<base> stride=4 count=<CMP bound> format="abs32" entries_mode="auto"`.

[[extra_func]]
addr = 0x080225B6
mode = "thumb"
note = "R3: proposed from self-heal miss-log; bridged x1; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x080225BE
mode = "thumb"
note = "R3: proposed from self-heal miss-log; bridged x1; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x080225D0
mode = "thumb"
note = "R3: proposed from self-heal miss-log; bridged x1; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x080225D6
mode = "thumb"
note = "R3: proposed from self-heal miss-log; bridged x1; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x080225E2
mode = "thumb"
note = "R3: proposed from self-heal miss-log; bridged x1; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x080225EA
mode = "thumb"
note = "R3: proposed from self-heal miss-log; bridged x1; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x080225FC
mode = "thumb"
note = "R3: proposed from self-heal miss-log; bridged x1; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08022602
mode = "thumb"
note = "R3: proposed from self-heal miss-log; bridged x1; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x0802260C
mode = "thumb"
note = "R3: proposed from self-heal miss-log; bridged x1; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08022616
mode = "thumb"
note = "R3: proposed from self-heal miss-log; bridged x1; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08022620
mode = "thumb"
note = "R3: proposed from self-heal miss-log; bridged x1; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08022624
mode = "thumb"
note = "R3: proposed from self-heal miss-log; bridged x1; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08022638
mode = "thumb"
note = "R3: proposed from self-heal miss-log; bridged x1; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x0802264C
mode = "thumb"
note = "R3: proposed from self-heal miss-log; bridged x1; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08022724
mode = "thumb"
note = "R3: proposed from self-heal miss-log; bridged x95"

[[extra_func]]
addr = 0x0802275A
mode = "thumb"
note = "R3: proposed from self-heal miss-log; bridged x350"

[[extra_func]]
addr = 0x080267EC
mode = "thumb"
note = "R3: proposed from self-heal miss-log; bridged x131"

# ── R3 JUMP-TABLE (unsized, admitted as roots): 15 consecutive thumb misses in [0x0802B386, 0x0802B47E] ──
# Likely the case targets of a computed-jump switch the finder
# could not size. PREFER one sized [[jump_table]] over the 15
# [[extra_func]] below: find the abs32 table base (the
# `ldr rT,[pc,#..]; add rT,index<<2; ldr/mov pc` dispatcher) and
# add `[[jump_table]] addr=<base> stride=4 count=<CMP bound> format="abs32" entries_mode="auto"`.

[[extra_func]]
addr = 0x0802B386
mode = "thumb"
note = "R3: proposed from self-heal miss-log; bridged x1; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x0802B3B2
mode = "thumb"
note = "R3: proposed from self-heal miss-log; bridged x1; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x0802B410
mode = "thumb"
note = "R3: proposed from self-heal miss-log; bridged x1; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x0802B424
mode = "thumb"
note = "R3: proposed from self-heal miss-log; bridged x1; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x0802B42A
mode = "thumb"
note = "R3: proposed from self-heal miss-log; bridged x1; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x0802B434
mode = "thumb"
note = "R3: proposed from self-heal miss-log; bridged x130; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x0802B43A
mode = "thumb"
note = "R3: proposed from self-heal miss-log; bridged x146; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x0802B452
mode = "thumb"
note = "R3: proposed from self-heal miss-log; bridged x1; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x0802B45A
mode = "thumb"
note = "R3: proposed from self-heal miss-log; bridged x1; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x0802B45E
mode = "thumb"
note = "R3: proposed from self-heal miss-log; bridged x5; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x0802B460
mode = "thumb"
note = "R3: proposed from self-heal miss-log; bridged x16; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x0802B466
mode = "thumb"
note = "R3: proposed from self-heal miss-log; bridged x16; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x0802B46C
mode = "thumb"
note = "R3: proposed from self-heal miss-log; bridged x16; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x0802B470
mode = "thumb"
note = "R3: proposed from self-heal miss-log; bridged x1; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x0802B47E
mode = "thumb"
note = "R3: proposed from self-heal miss-log; bridged x1; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x0813263A
mode = "thumb"
note = "R3: proposed from self-heal miss-log; bridged x1"

[[extra_func]]
addr = 0x081328A8
mode = "thumb"
note = "R3: proposed from self-heal miss-log; bridged x35"

[[extra_func]]
addr = 0x081328F8
mode = "thumb"
note = "R3: proposed from self-heal miss-log; bridged x8"

# ── R3 JUMP-TABLE (unsized, admitted as roots): 31 consecutive thumb misses in [0x08132CDA, 0x08132EE6] ──
# Likely the case targets of a computed-jump switch the finder
# could not size. PREFER one sized [[jump_table]] over the 31
# [[extra_func]] below: find the abs32 table base (the
# `ldr rT,[pc,#..]; add rT,index<<2; ldr/mov pc` dispatcher) and
# add `[[jump_table]] addr=<base> stride=4 count=<CMP bound> format="abs32" entries_mode="auto"`.

[[extra_func]]
addr = 0x08132CDA
mode = "thumb"
note = "R3: proposed from self-heal miss-log; bridged x9; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08132CF8
mode = "thumb"
note = "R3: proposed from self-heal miss-log; bridged x439; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08132D0A
mode = "thumb"
note = "R3: proposed from self-heal miss-log; bridged x92; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08132D0C
mode = "thumb"
note = "R3: proposed from self-heal miss-log; bridged x10; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08132D12
mode = "thumb"
note = "R3: proposed from self-heal miss-log; bridged x77; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08132D16
mode = "thumb"
note = "R3: proposed from self-heal miss-log; bridged x38; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08132D7C
mode = "thumb"
note = "R3: proposed from self-heal miss-log; bridged x1; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08132D88
mode = "thumb"
note = "R3: proposed from self-heal miss-log; bridged x2; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08132DA0
mode = "thumb"
note = "R3: proposed from self-heal miss-log; bridged x4; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08132DA8
mode = "thumb"
note = "R3: proposed from self-heal miss-log; bridged x1; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08132DC4
mode = "thumb"
note = "R3: proposed from self-heal miss-log; bridged x1; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08132E14
mode = "thumb"
note = "R3: proposed from self-heal miss-log; bridged x1; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08132E1A
mode = "thumb"
note = "R3: proposed from self-heal miss-log; bridged x1; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08132E28
mode = "thumb"
note = "R3: proposed from self-heal miss-log; bridged x7; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08132E2E
mode = "thumb"
note = "R3: proposed from self-heal miss-log; bridged x7; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08132E3C
mode = "thumb"
note = "R3: proposed from self-heal miss-log; bridged x6; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08132E56
mode = "thumb"
note = "R3: proposed from self-heal miss-log; bridged x7; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08132E5E
mode = "thumb"
note = "R3: proposed from self-heal miss-log; bridged x7; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08132E66
mode = "thumb"
note = "R3: proposed from self-heal miss-log; bridged x7; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08132E6C
mode = "thumb"
note = "R3: proposed from self-heal miss-log; bridged x3; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08132E72
mode = "thumb"
note = "R3: proposed from self-heal miss-log; bridged x20; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08132E80
mode = "thumb"
note = "R3: proposed from self-heal miss-log; bridged x1; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08132E86
mode = "thumb"
note = "R3: proposed from self-heal miss-log; bridged x21; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08132E94
mode = "thumb"
note = "R3: proposed from self-heal miss-log; bridged x6; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08132E9A
mode = "thumb"
note = "R3: proposed from self-heal miss-log; bridged x7; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08132EBC
mode = "thumb"
note = "R3: proposed from self-heal miss-log; bridged x2; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08132EC2
mode = "thumb"
note = "R3: proposed from self-heal miss-log; bridged x3; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08132ECE
mode = "thumb"
note = "R3: proposed from self-heal miss-log; bridged x3; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08132EDE
mode = "thumb"
note = "R3: proposed from self-heal miss-log; bridged x3; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08132EE0
mode = "thumb"
note = "R3: proposed from self-heal miss-log; bridged x1; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08132EE6
mode = "thumb"
note = "R3: proposed from self-heal miss-log; bridged x1; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

# ── R3 JUMP-TABLE (unsized, admitted as roots): 60 consecutive thumb misses in [0x08132FB0, 0x081334D8] ──
# Likely the case targets of a computed-jump switch the finder
# could not size. PREFER one sized [[jump_table]] over the 60
# [[extra_func]] below: find the abs32 table base (the
# `ldr rT,[pc,#..]; add rT,index<<2; ldr/mov pc` dispatcher) and
# add `[[jump_table]] addr=<base> stride=4 count=<CMP bound> format="abs32" entries_mode="auto"`.

[[extra_func]]
addr = 0x08132FB0
mode = "thumb"
note = "R3: proposed from self-heal miss-log; bridged x31; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08132FBA
mode = "thumb"
note = "R3: proposed from self-heal miss-log; bridged x25; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08132FC6
mode = "thumb"
note = "R3: proposed from self-heal miss-log; bridged x10; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08132FE2
mode = "thumb"
note = "R3: proposed from self-heal miss-log; bridged x2; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08132FE8
mode = "thumb"
note = "R3: proposed from self-heal miss-log; bridged x34; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08132FEE
mode = "thumb"
note = "R3: proposed from self-heal miss-log; bridged x16; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08132FFC
mode = "thumb"
note = "R3: proposed from self-heal miss-log; bridged x7; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08133014
mode = "thumb"
note = "R3: proposed from self-heal miss-log; bridged x11; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08133020
mode = "thumb"
note = "R3: proposed from self-heal miss-log; bridged x24; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x0813302A
mode = "thumb"
note = "R3: proposed from self-heal miss-log; bridged x15; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08133040
mode = "thumb"
note = "R3: proposed from self-heal miss-log; bridged x7; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x0813305A
mode = "thumb"
note = "R3: proposed from self-heal miss-log; bridged x16; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08133062
mode = "thumb"
note = "R3: proposed from self-heal miss-log; bridged x7; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x0813306C
mode = "thumb"
note = "R3: proposed from self-heal miss-log; bridged x14; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x0813308E
mode = "thumb"
note = "R3: proposed from self-heal miss-log; bridged x2; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x081330A6
mode = "thumb"
note = "R3: proposed from self-heal miss-log; bridged x2; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x081330C4
mode = "thumb"
note = "R3: proposed from self-heal miss-log; bridged x1; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x081330C8
mode = "thumb"
note = "R3: proposed from self-heal miss-log; bridged x25; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x081330CC
mode = "thumb"
note = "R3: proposed from self-heal miss-log; bridged x40; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x081330D8
mode = "thumb"
note = "R3: proposed from self-heal miss-log; bridged x32; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x081330EA
mode = "thumb"
note = "R3: proposed from self-heal miss-log; bridged x28; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x081330F8
mode = "thumb"
note = "R3: proposed from self-heal miss-log; bridged x27; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x081330FC
mode = "thumb"
note = "R3: proposed from self-heal miss-log; bridged x39; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08133114
mode = "thumb"
note = "R3: proposed from self-heal miss-log; bridged x7; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x0813311A
mode = "thumb"
note = "R3: proposed from self-heal miss-log; bridged x8; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x0813312A
mode = "thumb"
note = "R3: proposed from self-heal miss-log; bridged x8; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x0813313C
mode = "thumb"
note = "R3: proposed from self-heal miss-log; bridged x14; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08133148
mode = "thumb"
note = "R3: proposed from self-heal miss-log; bridged x11; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x0813318C
mode = "thumb"
note = "R3: proposed from self-heal miss-log; bridged x1; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08133196
mode = "thumb"
note = "R3: proposed from self-heal miss-log; bridged x13; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x081331A0
mode = "thumb"
note = "R3: proposed from self-heal miss-log; bridged x10; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x081331B0
mode = "thumb"
note = "R3: proposed from self-heal miss-log; bridged x7; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08133204
mode = "thumb"
note = "R3: proposed from self-heal miss-log; bridged x52; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x0813321C
mode = "thumb"
note = "R3: proposed from self-heal miss-log; bridged x66; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08133230
mode = "thumb"
note = "R3: proposed from self-heal miss-log; bridged x93; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08133234
mode = "thumb"
note = "R3: proposed from self-heal miss-log; bridged x5; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08133278
mode = "thumb"
note = "R3: proposed from self-heal miss-log; bridged x4; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x0813329A
mode = "thumb"
note = "R3: proposed from self-heal miss-log; bridged x1; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x081332B4
mode = "thumb"
note = "R3: proposed from self-heal miss-log; bridged x3; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x081332C8
mode = "thumb"
note = "R3: proposed from self-heal miss-log; bridged x4; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x081332CC
mode = "thumb"
note = "R3: proposed from self-heal miss-log; bridged x2; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x081332D0
mode = "thumb"
note = "R3: proposed from self-heal miss-log; bridged x1; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x081332E0
mode = "thumb"
note = "R3: proposed from self-heal miss-log; bridged x4; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x081332F8
mode = "thumb"
note = "R3: proposed from self-heal miss-log; bridged x6; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08133320
mode = "thumb"
note = "R3: proposed from self-heal miss-log; bridged x2; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x0813332E
mode = "thumb"
note = "R3: proposed from self-heal miss-log; bridged x1; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08133366
mode = "thumb"
note = "R3: proposed from self-heal miss-log; bridged x1; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08133374
mode = "thumb"
note = "R3: proposed from self-heal miss-log; bridged x4; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x0813337A
mode = "thumb"
note = "R3: proposed from self-heal miss-log; bridged x47; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08133388
mode = "thumb"
note = "R3: proposed from self-heal miss-log; bridged x59; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x0813339A
mode = "thumb"
note = "R3: proposed from self-heal miss-log; bridged x29; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x081333A2
mode = "thumb"
note = "R3: proposed from self-heal miss-log; bridged x74; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x081333C8
mode = "thumb"
note = "R3: proposed from self-heal miss-log; bridged x27; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x081333D4
mode = "thumb"
note = "R3: proposed from self-heal miss-log; bridged x43; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x081333EE
mode = "thumb"
note = "R3: proposed from self-heal miss-log; bridged x27; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08133402
mode = "thumb"
note = "R3: proposed from self-heal miss-log; bridged x15; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08133474
mode = "thumb"
note = "R3: proposed from self-heal miss-log; bridged x25; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08133486
mode = "thumb"
note = "R3: proposed from self-heal miss-log; bridged x34; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x081334C4
mode = "thumb"
note = "R3: proposed from self-heal miss-log; bridged x7; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x081334D8
mode = "thumb"
note = "R3: proposed from self-heal miss-log; bridged x1; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x0813360C
mode = "thumb"
note = "R3: proposed from self-heal miss-log; bridged x1"

[[extra_func]]
addr = 0x08133940
mode = "thumb"
note = "R3: proposed from self-heal miss-log; bridged x1"

[[extra_func]]
addr = 0x0813394A
mode = "thumb"
note = "R3: proposed from self-heal miss-log; bridged x34"

[[extra_func]]
addr = 0x08133DF2
mode = "thumb"
note = "R3: proposed from self-heal miss-log; bridged x1"

# ── R3 JUMP-TABLE (unsized, admitted as roots): 13 consecutive thumb misses in [0x08133F0C, 0x08134004] ──
# Likely the case targets of a computed-jump switch the finder
# could not size. PREFER one sized [[jump_table]] over the 13
# [[extra_func]] below: find the abs32 table base (the
# `ldr rT,[pc,#..]; add rT,index<<2; ldr/mov pc` dispatcher) and
# add `[[jump_table]] addr=<base> stride=4 count=<CMP bound> format="abs32" entries_mode="auto"`.

[[extra_func]]
addr = 0x08133F0C
mode = "thumb"
note = "R3: proposed from self-heal miss-log; bridged x70; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08133F34
mode = "thumb"
note = "R3: proposed from self-heal miss-log; bridged x6; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08133F4A
mode = "thumb"
note = "R3: proposed from self-heal miss-log; bridged x3; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08133F56
mode = "thumb"
note = "R3: proposed from self-heal miss-log; bridged x4; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08133F5C
mode = "thumb"
note = "R3: proposed from self-heal miss-log; bridged x4; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08133F70
mode = "thumb"
note = "R3: proposed from self-heal miss-log; bridged x79; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08133FAE
mode = "thumb"
note = "R3: proposed from self-heal miss-log; bridged x20; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08133FB4
mode = "thumb"
note = "R3: proposed from self-heal miss-log; bridged x25; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08133FC0
mode = "thumb"
note = "R3: proposed from self-heal miss-log; bridged x20; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08133FDC
mode = "thumb"
note = "R3: proposed from self-heal miss-log; bridged x7; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08133FEA
mode = "thumb"
note = "R3: proposed from self-heal miss-log; bridged x37; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08133FF8
mode = "thumb"
note = "R3: proposed from self-heal miss-log; bridged x10; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08134004
mode = "thumb"
note = "R3: proposed from self-heal miss-log; bridged x127; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

# ── R3 JUMP-TABLE (unsized, admitted as roots): 3 consecutive thumb misses in [0x08134120, 0x0813414A] ──
# Likely the case targets of a computed-jump switch the finder
# could not size. PREFER one sized [[jump_table]] over the 3
# [[extra_func]] below: find the abs32 table base (the
# `ldr rT,[pc,#..]; add rT,index<<2; ldr/mov pc` dispatcher) and
# add `[[jump_table]] addr=<base> stride=4 count=<CMP bound> format="abs32" entries_mode="auto"`.

[[extra_func]]
addr = 0x08134120
mode = "thumb"
note = "R3: proposed from self-heal miss-log; bridged x32; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08134144
mode = "thumb"
note = "R3: proposed from self-heal miss-log; bridged x1; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x0813414A
mode = "thumb"
note = "R3: proposed from self-heal miss-log; bridged x7; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

# Round 4: boot frontier, 31 entries. Held (no source, likely speculative): 0x03007AF0, 0x03007B44.

[[code_copy]]
runtime_start = 0x0300416E
source_start = 0x0813293A
size = 0x00000403
name = "iwram_copy_2"
note = "R4: maximal exact live-IWRAM/ROM run; entry 0x03004170"

[[extra_func]]
addr = 0x03004170
mode = "thumb"
note = "R4: proposed from self-heal miss-log; bridged x3253"

# ── R4 JUMP-TABLE (unsized, admitted as roots): 3 consecutive thumb misses in [0x03005F2C, 0x03005F94] ──
# Likely the case targets of a computed-jump switch the finder
# could not size. PREFER one sized [[jump_table]] over the 3
# [[extra_func]] below: find the abs32 table base (the
# `ldr rT,[pc,#..]; add rT,index<<2; ldr/mov pc` dispatcher) and
# add `[[jump_table]] addr=<base> stride=4 count=<CMP bound> format="abs32" entries_mode="auto"`.

[[extra_func]]
addr = 0x03005F2C
mode = "thumb"
note = "R4: proposed from self-heal miss-log; bridged x674754; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x03005F38
mode = "thumb"
note = "R4: proposed from self-heal miss-log; bridged x3256; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x03005F94
mode = "thumb"
note = "R4: proposed from self-heal miss-log; bridged x3253; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08000322
mode = "thumb"
note = "R4: proposed from self-heal miss-log; bridged x78"

[[extra_func]]
addr = 0x08001DB8
mode = "thumb"
note = "R4: proposed from self-heal miss-log; bridged x2"

[[extra_func]]
addr = 0x081327DC
mode = "thumb"
note = "R4: proposed from self-heal miss-log; bridged x6"

[[extra_func]]
addr = 0x08132EC8
mode = "thumb"
note = "R4: proposed from self-heal miss-log; bridged x3"

# ── R4 JUMP-TABLE (unsized, admitted as roots): 6 consecutive thumb misses in [0x08132F58, 0x08132FA0] ──
# Likely the case targets of a computed-jump switch the finder
# could not size. PREFER one sized [[jump_table]] over the 6
# [[extra_func]] below: find the abs32 table base (the
# `ldr rT,[pc,#..]; add rT,index<<2; ldr/mov pc` dispatcher) and
# add `[[jump_table]] addr=<base> stride=4 count=<CMP bound> format="abs32" entries_mode="auto"`.

[[extra_func]]
addr = 0x08132F58
mode = "thumb"
note = "R4: proposed from self-heal miss-log; bridged x864; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08132F62
mode = "thumb"
note = "R4: proposed from self-heal miss-log; bridged x2304; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08132F74
mode = "thumb"
note = "R4: proposed from self-heal miss-log; bridged x1488; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08132F8C
mode = "thumb"
note = "R4: proposed from self-heal miss-log; bridged x18; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08132F98
mode = "thumb"
note = "R4: proposed from self-heal miss-log; bridged x17; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08132FA0
mode = "thumb"
note = "R4: proposed from self-heal miss-log; bridged x18; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x081330F0
mode = "thumb"
note = "R4: proposed from self-heal miss-log; bridged x18"

# ── R4 JUMP-TABLE (unsized, admitted as roots): 3 consecutive thumb misses in [0x08133490, 0x081334B0] ──
# Likely the case targets of a computed-jump switch the finder
# could not size. PREFER one sized [[jump_table]] over the 3
# [[extra_func]] below: find the abs32 table base (the
# `ldr rT,[pc,#..]; add rT,index<<2; ldr/mov pc` dispatcher) and
# add `[[jump_table]] addr=<base> stride=4 count=<CMP bound> format="abs32" entries_mode="auto"`.

[[extra_func]]
addr = 0x08133490
mode = "thumb"
note = "R4: proposed from self-heal miss-log; bridged x6; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x0813349C
mode = "thumb"
note = "R4: proposed from self-heal miss-log; bridged x3; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x081334B0
mode = "thumb"
note = "R4: proposed from self-heal miss-log; bridged x3; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

# ── R4 JUMP-TABLE (unsized, admitted as roots): 8 consecutive thumb misses in [0x08133E44, 0x08133F06] ──
# Likely the case targets of a computed-jump switch the finder
# could not size. PREFER one sized [[jump_table]] over the 8
# [[extra_func]] below: find the abs32 table base (the
# `ldr rT,[pc,#..]; add rT,index<<2; ldr/mov pc` dispatcher) and
# add `[[jump_table]] addr=<base> stride=4 count=<CMP bound> format="abs32" entries_mode="auto"`.

[[extra_func]]
addr = 0x08133E44
mode = "thumb"
note = "R4: proposed from self-heal miss-log; bridged x87; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08133E88
mode = "thumb"
note = "R4: proposed from self-heal miss-log; bridged x2; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08133E9E
mode = "thumb"
note = "R4: proposed from self-heal miss-log; bridged x1; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08133EA6
mode = "thumb"
note = "R4: proposed from self-heal miss-log; bridged x8; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08133EDA
mode = "thumb"
note = "R4: proposed from self-heal miss-log; bridged x2; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08133EE8
mode = "thumb"
note = "R4: proposed from self-heal miss-log; bridged x2; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08133EFE
mode = "thumb"
note = "R4: proposed from self-heal miss-log; bridged x2; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08133F06
mode = "thumb"
note = "R4: proposed from self-heal miss-log; bridged x17; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08134938
mode = "thumb"
note = "R4: proposed from self-heal miss-log; bridged x6"

# Round 5: two cart Thumb roots at the advancing 0x8134Axx frontier (x3 each).
# 0x03007AF0/0x03007B44 recur (2nd sighting, x6) but remain sourceless in the
# interp IWRAM dump; still held per miss-cheap/false-positive-expensive.

[[extra_func]]
addr = 0x08134A18
mode = "thumb"
note = "R5: advancing cart frontier, bridged x3"

[[extra_func]]
addr = 0x08134A24
mode = "thumb"
note = "R5: advancing cart frontier, bridged x3"

# Round 6: demo-campaign frontier, 305 new entries (skipped 0 already-seeded; held 0x03007AF0, 0x03007B44). IWRAM 0x60xx roots decode via iwram_sound_mixer span.

# ── R6 JUMP-TABLE (unsized, admitted as roots): 4 consecutive thumb misses in [0x0300604A, 0x030060D0] ──
# Likely the case targets of a computed-jump switch the finder
# could not size. PREFER one sized [[jump_table]] over the 4
# [[extra_func]] below: find the abs32 table base (the
# `ldr rT,[pc,#..]; add rT,index<<2; ldr/mov pc` dispatcher) and
# add `[[jump_table]] addr=<base> stride=4 count=<CMP bound> format="abs32" entries_mode="auto"`.

[[extra_func]]
addr = 0x0300604A
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x2; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x03006050
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x1; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x0300608A
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x61344; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x030060D0
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x334; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x03006182
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x102"

[[extra_func]]
addr = 0x030063D6
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x5090"

[[extra_func]]
addr = 0x0300664C
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x24080"

# ── R6 JUMP-TABLE (unsized, admitted as roots): 3 consecutive thumb misses in [0x030069A4, 0x03006A6A] ──
# Likely the case targets of a computed-jump switch the finder
# could not size. PREFER one sized [[jump_table]] over the 3
# [[extra_func]] below: find the abs32 table base (the
# `ldr rT,[pc,#..]; add rT,index<<2; ldr/mov pc` dispatcher) and
# add `[[jump_table]] addr=<base> stride=4 count=<CMP bound> format="abs32" entries_mode="auto"`.

[[extra_func]]
addr = 0x030069A4
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x72; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x030069EE
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x44; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x03006A6A
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x28; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

# ── R6 JUMP-TABLE (unsized, admitted as roots): 3 consecutive arm misses in [0x08000120, 0x08000148] ──
# Likely the case targets of a computed-jump switch the finder
# could not size. PREFER one sized [[jump_table]] over the 3
# [[extra_func]] below: find the abs32 table base (the
# `ldr rT,[pc,#..]; add rT,index<<2; ldr/mov pc` dispatcher) and
# add `[[jump_table]] addr=<base> stride=4 count=<CMP bound> format="abs32" entries_mode="auto"`.

[[extra_func]]
addr = 0x08000120
mode = "arm"
note = "R6: proposed from self-heal miss-log; bridged x1; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x0800012C
mode = "arm"
note = "R6: proposed from self-heal miss-log; bridged x1; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08000148
mode = "arm"
note = "R6: proposed from self-heal miss-log; bridged x1; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x080001D8
mode = "arm"
note = "R6: proposed from self-heal miss-log; bridged x2"

# ── R6 JUMP-TABLE (unsized, admitted as roots): 5 consecutive thumb misses in [0x08000364, 0x0800036C] ──
# Likely the case targets of a computed-jump switch the finder
# could not size. PREFER one sized [[jump_table]] over the 5
# [[extra_func]] below: find the abs32 table base (the
# `ldr rT,[pc,#..]; add rT,index<<2; ldr/mov pc` dispatcher) and
# add `[[jump_table]] addr=<base> stride=4 count=<CMP bound> format="abs32" entries_mode="auto"`.

[[extra_func]]
addr = 0x08000364
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x95; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08000366
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x72; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08000368
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x79; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x0800036A
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x72; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x0800036C
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x11; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08000554
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x237"

[[extra_func]]
addr = 0x08000556
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x64"

# ── R6 JUMP-TABLE (unsized, admitted as roots): 3 consecutive thumb misses in [0x080006E2, 0x0800074E] ──
# Likely the case targets of a computed-jump switch the finder
# could not size. PREFER one sized [[jump_table]] over the 3
# [[extra_func]] below: find the abs32 table base (the
# `ldr rT,[pc,#..]; add rT,index<<2; ldr/mov pc` dispatcher) and
# add `[[jump_table]] addr=<base> stride=4 count=<CMP bound> format="abs32" entries_mode="auto"`.

[[extra_func]]
addr = 0x080006E2
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x5; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x0800073C
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x1; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x0800074E
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x4; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x080009E2
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x2"

[[extra_func]]
addr = 0x08001E0C
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x8"

[[extra_func]]
addr = 0x08001FE2
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x4"

[[extra_func]]
addr = 0x080020B0
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x59"

[[extra_func]]
addr = 0x080020BE
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x30"

[[extra_func]]
addr = 0x0800240C
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x273"

# ── R6 JUMP-TABLE (unsized, admitted as roots): 3 consecutive thumb misses in [0x080025D0, 0x08002642] ──
# Likely the case targets of a computed-jump switch the finder
# could not size. PREFER one sized [[jump_table]] over the 3
# [[extra_func]] below: find the abs32 table base (the
# `ldr rT,[pc,#..]; add rT,index<<2; ldr/mov pc` dispatcher) and
# add `[[jump_table]] addr=<base> stride=4 count=<CMP bound> format="abs32" entries_mode="auto"`.

[[extra_func]]
addr = 0x080025D0
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x7294; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x080025E6
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x123; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08002642
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x5156; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

# ── R6 JUMP-TABLE (unsized, admitted as roots): 3 consecutive thumb misses in [0x08002826, 0x080028AC] ──
# Likely the case targets of a computed-jump switch the finder
# could not size. PREFER one sized [[jump_table]] over the 3
# [[extra_func]] below: find the abs32 table base (the
# `ldr rT,[pc,#..]; add rT,index<<2; ldr/mov pc` dispatcher) and
# add `[[jump_table]] addr=<base> stride=4 count=<CMP bound> format="abs32" entries_mode="auto"`.

[[extra_func]]
addr = 0x08002826
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x4; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08002848
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x4; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x080028AC
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x20; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08002B64
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x5090"

[[extra_func]]
addr = 0x0800341E
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x272"

[[extra_func]]
addr = 0x08003616
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x509"

# ── R6 JUMP-TABLE (unsized, admitted as roots): 3 consecutive thumb misses in [0x08003D8A, 0x08003E32] ──
# Likely the case targets of a computed-jump switch the finder
# could not size. PREFER one sized [[jump_table]] over the 3
# [[extra_func]] below: find the abs32 table base (the
# `ldr rT,[pc,#..]; add rT,index<<2; ldr/mov pc` dispatcher) and
# add `[[jump_table]] addr=<base> stride=4 count=<CMP bound> format="abs32" entries_mode="auto"`.

[[extra_func]]
addr = 0x08003D8A
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x9; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08003DCA
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x272; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08003E32
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x4581; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

# ── R6 JUMP-TABLE (unsized, admitted as roots): 21 consecutive thumb misses in [0x080046A4, 0x08004816] ──
# Likely the case targets of a computed-jump switch the finder
# could not size. PREFER one sized [[jump_table]] over the 21
# [[extra_func]] below: find the abs32 table base (the
# `ldr rT,[pc,#..]; add rT,index<<2; ldr/mov pc` dispatcher) and
# add `[[jump_table]] addr=<base> stride=4 count=<CMP bound> format="abs32" entries_mode="auto"`.

[[extra_func]]
addr = 0x080046A4
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x514; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x080046B8
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x514; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x080046F6
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x1; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08004712
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x4; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08004718
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x4; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x0800471C
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x4; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08004720
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x4; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08004730
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x4; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08004738
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x4; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x0800473C
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x4; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08004744
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x4; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x0800474E
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x4; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08004776
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x4; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x0800477E
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x4; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08004796
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x4; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x0800479E
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x4; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x080047B6
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x4; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x080047BE
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x4; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x080047E6
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x4; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x080047F6
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x4; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08004816
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x272; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

# ── R6 JUMP-TABLE (unsized, admitted as roots): 7 consecutive thumb misses in [0x080049E0, 0x08004A5A] ──
# Likely the case targets of a computed-jump switch the finder
# could not size. PREFER one sized [[jump_table]] over the 7
# [[extra_func]] below: find the abs32 table base (the
# `ldr rT,[pc,#..]; add rT,index<<2; ldr/mov pc` dispatcher) and
# add `[[jump_table]] addr=<base> stride=4 count=<CMP bound> format="abs32" entries_mode="auto"`.

[[extra_func]]
addr = 0x080049E0
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x4; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08004A22
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x64; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08004A40
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x4; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08004A4E
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x4; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08004A52
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x4; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08004A56
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x4; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08004A5A
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x4; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

# ── R6 JUMP-TABLE (unsized, admitted as roots): 3 consecutive thumb misses in [0x08004B8A, 0x08004BB0] ──
# Likely the case targets of a computed-jump switch the finder
# could not size. PREFER one sized [[jump_table]] over the 3
# [[extra_func]] below: find the abs32 table base (the
# `ldr rT,[pc,#..]; add rT,index<<2; ldr/mov pc` dispatcher) and
# add `[[jump_table]] addr=<base> stride=4 count=<CMP bound> format="abs32" entries_mode="auto"`.

[[extra_func]]
addr = 0x08004B8A
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x270; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08004BA2
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x270; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08004BB0
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x98; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

# ── R6 JUMP-TABLE (unsized, admitted as roots): 6 consecutive thumb misses in [0x08004CCE, 0x08004D74] ──
# Likely the case targets of a computed-jump switch the finder
# could not size. PREFER one sized [[jump_table]] over the 6
# [[extra_func]] below: find the abs32 table base (the
# `ldr rT,[pc,#..]; add rT,index<<2; ldr/mov pc` dispatcher) and
# add `[[jump_table]] addr=<base> stride=4 count=<CMP bound> format="abs32" entries_mode="auto"`.

[[extra_func]]
addr = 0x08004CCE
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x270; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08004CE6
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x270; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08004CF4
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x98; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08004D46
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x267; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08004D66
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x267; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08004D74
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x98; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08007CFA
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x1"

[[extra_func]]
addr = 0x080146CA
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x98"

# ── R6 JUMP-TABLE (unsized, admitted as roots): 3 consecutive thumb misses in [0x08021CEA, 0x08021D48] ──
# Likely the case targets of a computed-jump switch the finder
# could not size. PREFER one sized [[jump_table]] over the 3
# [[extra_func]] below: find the abs32 table base (the
# `ldr rT,[pc,#..]; add rT,index<<2; ldr/mov pc` dispatcher) and
# add `[[jump_table]] addr=<base> stride=4 count=<CMP bound> format="abs32" entries_mode="auto"`.

[[extra_func]]
addr = 0x08021CEA
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x4; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08021CFE
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x4; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08021D48
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x1; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08021DE2
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x6"

[[extra_func]]
addr = 0x08021DEE
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x6"

# ── R6 JUMP-TABLE (unsized, admitted as roots): 4 consecutive thumb misses in [0x08022060, 0x08022084] ──
# Likely the case targets of a computed-jump switch the finder
# could not size. PREFER one sized [[jump_table]] over the 4
# [[extra_func]] below: find the abs32 table base (the
# `ldr rT,[pc,#..]; add rT,index<<2; ldr/mov pc` dispatcher) and
# add `[[jump_table]] addr=<base> stride=4 count=<CMP bound> format="abs32" entries_mode="auto"`.

[[extra_func]]
addr = 0x08022060
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x1; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x0802207C
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x1; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08022080
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x1; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08022084
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x1; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x0802212C
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x12"

[[extra_func]]
addr = 0x08022136
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x12"

# ── R6 JUMP-TABLE (unsized, admitted as roots): 4 consecutive thumb misses in [0x080222A2, 0x080222BA] ──
# Likely the case targets of a computed-jump switch the finder
# could not size. PREFER one sized [[jump_table]] over the 4
# [[extra_func]] below: find the abs32 table base (the
# `ldr rT,[pc,#..]; add rT,index<<2; ldr/mov pc` dispatcher) and
# add `[[jump_table]] addr=<base> stride=4 count=<CMP bound> format="abs32" entries_mode="auto"`.

[[extra_func]]
addr = 0x080222A2
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x1; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x080222A8
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x1; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x080222AE
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x1; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x080222BA
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x1; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x080228A2
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x4"

# ── R6 JUMP-TABLE (unsized, admitted as roots): 7 consecutive thumb misses in [0x08022CB6, 0x08022D14] ──
# Likely the case targets of a computed-jump switch the finder
# could not size. PREFER one sized [[jump_table]] over the 7
# [[extra_func]] below: find the abs32 table base (the
# `ldr rT,[pc,#..]; add rT,index<<2; ldr/mov pc` dispatcher) and
# add `[[jump_table]] addr=<base> stride=4 count=<CMP bound> format="abs32" entries_mode="auto"`.

[[extra_func]]
addr = 0x08022CB6
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x4; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08022CBC
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x4; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08022CD2
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x12; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08022CE8
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x12; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08022CEE
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x12; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08022D08
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x4; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08022D14
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x4; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x080233F6
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x4"

[[extra_func]]
addr = 0x080234D2
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x301"

[[extra_func]]
addr = 0x08024240
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x2891"

[[extra_func]]
addr = 0x08024244
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x2891"

[[extra_func]]
addr = 0x0802443C
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x201"

[[extra_func]]
addr = 0x08024440
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x201"

[[extra_func]]
addr = 0x0802664C
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x4"

# ── R6 JUMP-TABLE (unsized, admitted as roots): 6 consecutive thumb misses in [0x0802672E, 0x080267E8] ──
# Likely the case targets of a computed-jump switch the finder
# could not size. PREFER one sized [[jump_table]] over the 6
# [[extra_func]] below: find the abs32 table base (the
# `ldr rT,[pc,#..]; add rT,index<<2; ldr/mov pc` dispatcher) and
# add `[[jump_table]] addr=<base> stride=4 count=<CMP bound> format="abs32" entries_mode="auto"`.

[[extra_func]]
addr = 0x0802672E
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x1; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08026766
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x1; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x0802678A
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x1; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x080267A6
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x1; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x080267B0
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x1; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x080267E8
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x8; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08026962
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x56"

[[extra_func]]
addr = 0x08026FB8
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x1"

[[extra_func]]
addr = 0x0802700C
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x28"

[[extra_func]]
addr = 0x0802724C
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x8"

[[extra_func]]
addr = 0x08027270
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x28"

# ── R6 JUMP-TABLE (unsized, admitted as roots): 6 consecutive thumb misses in [0x0802938E, 0x080293F8] ──
# Likely the case targets of a computed-jump switch the finder
# could not size. PREFER one sized [[jump_table]] over the 6
# [[extra_func]] below: find the abs32 table base (the
# `ldr rT,[pc,#..]; add rT,index<<2; ldr/mov pc` dispatcher) and
# add `[[jump_table]] addr=<base> stride=4 count=<CMP bound> format="abs32" entries_mode="auto"`.

[[extra_func]]
addr = 0x0802938E
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x4; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x080293A2
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x4; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x080293D6
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x7; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x080293E4
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x7; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x080293EE
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x7; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x080293F8
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x7; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x0802954E
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x134"

[[extra_func]]
addr = 0x0802959A
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x134"

[[extra_func]]
addr = 0x0802A086
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x103"

[[extra_func]]
addr = 0x0802A094
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x98"

[[extra_func]]
addr = 0x0802A27A
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x103"

[[extra_func]]
addr = 0x0802A288
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x98"

[[extra_func]]
addr = 0x0802A32A
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x103"

[[extra_func]]
addr = 0x0802AD00
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x1"

[[extra_func]]
addr = 0x0802B25E
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x276"

[[extra_func]]
addr = 0x0802B296
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x272"

# ── R6 JUMP-TABLE (unsized, admitted as roots): 7 consecutive thumb misses in [0x0802B75A, 0x0802B7F2] ──
# Likely the case targets of a computed-jump switch the finder
# could not size. PREFER one sized [[jump_table]] over the 7
# [[extra_func]] below: find the abs32 table base (the
# `ldr rT,[pc,#..]; add rT,index<<2; ldr/mov pc` dispatcher) and
# add `[[jump_table]] addr=<base> stride=4 count=<CMP bound> format="abs32" entries_mode="auto"`.

[[extra_func]]
addr = 0x0802B75A
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x4; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x0802B76C
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x4; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x0802B772
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x4; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x0802B794
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x3; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x0802B7DE
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x7; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x0802B7E8
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x7; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x0802B7F2
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x7; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

# ── R6 JUMP-TABLE (unsized, admitted as roots): 11 consecutive thumb misses in [0x0802B886, 0x0802B9A8] ──
# Likely the case targets of a computed-jump switch the finder
# could not size. PREFER one sized [[jump_table]] over the 11
# [[extra_func]] below: find the abs32 table base (the
# `ldr rT,[pc,#..]; add rT,index<<2; ldr/mov pc` dispatcher) and
# add `[[jump_table]] addr=<base> stride=4 count=<CMP bound> format="abs32" entries_mode="auto"`.

[[extra_func]]
addr = 0x0802B886
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x134; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x0802B89E
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x3; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x0802B8C4
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x4; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x0802B920
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x3; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x0802B92C
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x3; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x0802B942
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x3; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x0802B948
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x3; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x0802B950
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x3; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x0802B96A
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x3; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x0802B984
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x68; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x0802B9A8
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x237; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x0802BB34
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x10"

# ── R6 JUMP-TABLE (unsized, admitted as roots): 3 consecutive thumb misses in [0x0802BCC0, 0x0802BCF2] ──
# Likely the case targets of a computed-jump switch the finder
# could not size. PREFER one sized [[jump_table]] over the 3
# [[extra_func]] below: find the abs32 table base (the
# `ldr rT,[pc,#..]; add rT,index<<2; ldr/mov pc` dispatcher) and
# add `[[jump_table]] addr=<base> stride=4 count=<CMP bound> format="abs32" entries_mode="auto"`.

[[extra_func]]
addr = 0x0802BCC0
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x2204; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x0802BCD0
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x199; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x0802BCF2
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x21; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

# ── R6 JUMP-TABLE (unsized, admitted as roots): 3 consecutive thumb misses in [0x0802C1D2, 0x0802C2A2] ──
# Likely the case targets of a computed-jump switch the finder
# could not size. PREFER one sized [[jump_table]] over the 3
# [[extra_func]] below: find the abs32 table base (the
# `ldr rT,[pc,#..]; add rT,index<<2; ldr/mov pc` dispatcher) and
# add `[[jump_table]] addr=<base> stride=4 count=<CMP bound> format="abs32" entries_mode="auto"`.

[[extra_func]]
addr = 0x0802C1D2
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x1539; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x0802C23A
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x1029; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x0802C2A2
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x1608; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x0802C32A
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x855"

# ── R6 JUMP-TABLE (unsized, admitted as roots): 12 consecutive thumb misses in [0x08032DBA, 0x08032EB4] ──
# Likely the case targets of a computed-jump switch the finder
# could not size. PREFER one sized [[jump_table]] over the 12
# [[extra_func]] below: find the abs32 table base (the
# `ldr rT,[pc,#..]; add rT,index<<2; ldr/mov pc` dispatcher) and
# add `[[jump_table]] addr=<base> stride=4 count=<CMP bound> format="abs32" entries_mode="auto"`.

[[extra_func]]
addr = 0x08032DBA
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x4; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08032DE0
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x4; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08032DE4
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x4; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08032DE8
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x4; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08032E00
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x187; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08032E14
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x187; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08032E88
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x3; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08032E92
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x3; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08032E9C
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x3; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08032EA2
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x3; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08032EAC
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x3; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08032EB4
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x3; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

# ── R6 JUMP-TABLE (unsized, admitted as roots): 18 consecutive thumb misses in [0x08033032, 0x080330E0] ──
# Likely the case targets of a computed-jump switch the finder
# could not size. PREFER one sized [[jump_table]] over the 18
# [[extra_func]] below: find the abs32 table base (the
# `ldr rT,[pc,#..]; add rT,index<<2; ldr/mov pc` dispatcher) and
# add `[[jump_table]] addr=<base> stride=4 count=<CMP bound> format="abs32" entries_mode="auto"`.

[[extra_func]]
addr = 0x08033032
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x4; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x0803303C
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x4; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08033046
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x4; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08033050
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x4; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x0803305A
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x4; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08033064
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x4; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x0803306E
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x4; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08033078
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x4; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08033082
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x4; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x0803308E
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x4; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x0803309A
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x4; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x080330A4
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x4; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x080330AE
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x4; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x080330B8
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x4; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x080330C4
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x4; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x080330CE
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x4; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x080330D8
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x4; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x080330E0
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x4; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08033186
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x4"

[[extra_func]]
addr = 0x08033292
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x183"

# ── R6 JUMP-TABLE (unsized, admitted as roots): 3 consecutive thumb misses in [0x080333FE, 0x0803341A] ──
# Likely the case targets of a computed-jump switch the finder
# could not size. PREFER one sized [[jump_table]] over the 3
# [[extra_func]] below: find the abs32 table base (the
# `ldr rT,[pc,#..]; add rT,index<<2; ldr/mov pc` dispatcher) and
# add `[[jump_table]] addr=<base> stride=4 count=<CMP bound> format="abs32" entries_mode="auto"`.

[[extra_func]]
addr = 0x080333FE
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x4; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08033410
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x4; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x0803341A
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x4; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

# ── R6 JUMP-TABLE (unsized, admitted as roots): 11 consecutive thumb misses in [0x0803356C, 0x080336E6] ──
# Likely the case targets of a computed-jump switch the finder
# could not size. PREFER one sized [[jump_table]] over the 11
# [[extra_func]] below: find the abs32 table base (the
# `ldr rT,[pc,#..]; add rT,index<<2; ldr/mov pc` dispatcher) and
# add `[[jump_table]] addr=<base> stride=4 count=<CMP bound> format="abs32" entries_mode="auto"`.

[[extra_func]]
addr = 0x0803356C
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x28; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08033582
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x4; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x080335B0
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x28; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x080335E2
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x4; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x080335F2
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x4; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08033610
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x28; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x0803361A
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x28; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08033642
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x4; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x0803368A
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x4; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x080336A8
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x28; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x080336E6
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x4; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

# ── R6 JUMP-TABLE (unsized, admitted as roots): 4 consecutive thumb misses in [0x0803395A, 0x080339B4] ──
# Likely the case targets of a computed-jump switch the finder
# could not size. PREFER one sized [[jump_table]] over the 4
# [[extra_func]] below: find the abs32 table base (the
# `ldr rT,[pc,#..]; add rT,index<<2; ldr/mov pc` dispatcher) and
# add `[[jump_table]] addr=<base> stride=4 count=<CMP bound> format="abs32" entries_mode="auto"`.

[[extra_func]]
addr = 0x0803395A
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x4; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08033964
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x4; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x0803396C
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x4; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x080339B4
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x4; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

# ── R6 JUMP-TABLE (unsized, admitted as roots): 9 consecutive thumb misses in [0x08033FB4, 0x08034162] ──
# Likely the case targets of a computed-jump switch the finder
# could not size. PREFER one sized [[jump_table]] over the 9
# [[extra_func]] below: find the abs32 table base (the
# `ldr rT,[pc,#..]; add rT,index<<2; ldr/mov pc` dispatcher) and
# add `[[jump_table]] addr=<base> stride=4 count=<CMP bound> format="abs32" entries_mode="auto"`.

[[extra_func]]
addr = 0x08033FB4
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x4; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08033FBC
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x4; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08034008
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x183; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08034080
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x4; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x080340C2
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x4; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x080340DE
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x4; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x080340E6
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x4; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08034142
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x135; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08034162
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x48; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x080342E4
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x4"

[[extra_func]]
addr = 0x08034308
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x4"

[[extra_func]]
addr = 0x08034520
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x3"

[[extra_func]]
addr = 0x0803452A
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x3"

[[extra_func]]
addr = 0x08034612
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x183"

[[extra_func]]
addr = 0x0803BA72
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x4"

[[extra_func]]
addr = 0x0803BA7C
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x4"

# ── R6 JUMP-TABLE (unsized, admitted as roots): 4 consecutive thumb misses in [0x08045F2C, 0x08045F68] ──
# Likely the case targets of a computed-jump switch the finder
# could not size. PREFER one sized [[jump_table]] over the 4
# [[extra_func]] below: find the abs32 table base (the
# `ldr rT,[pc,#..]; add rT,index<<2; ldr/mov pc` dispatcher) and
# add `[[jump_table]] addr=<base> stride=4 count=<CMP bound> format="abs32" entries_mode="auto"`.

[[extra_func]]
addr = 0x08045F2C
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x4; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08045F3E
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x4; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08045F4A
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x4; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08045F68
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x4; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x080475C8
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x4"

[[extra_func]]
addr = 0x080475E4
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x509"

[[extra_func]]
addr = 0x0804770C
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x4"

[[extra_func]]
addr = 0x08047726
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x509"

# ── R6 JUMP-TABLE (unsized, admitted as roots): 8 consecutive thumb misses in [0x080613AE, 0x08061480] ──
# Likely the case targets of a computed-jump switch the finder
# could not size. PREFER one sized [[jump_table]] over the 8
# [[extra_func]] below: find the abs32 table base (the
# `ldr rT,[pc,#..]; add rT,index<<2; ldr/mov pc` dispatcher) and
# add `[[jump_table]] addr=<base> stride=4 count=<CMP bound> format="abs32" entries_mode="auto"`.

[[extra_func]]
addr = 0x080613AE
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x4; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x080613B6
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x4; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x080613BE
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x4; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x080613C6
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x4; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x080613D8
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x4; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08061412
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x4; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08061462
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x103; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08061480
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x103; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x080ABD92
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x4"

[[extra_func]]
addr = 0x080ABE74
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x272"

[[extra_func]]
addr = 0x080AC07E
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x10"

# ── R6 JUMP-TABLE (unsized, admitted as roots): 3 consecutive thumb misses in [0x080AC2F4, 0x080AC36E] ──
# Likely the case targets of a computed-jump switch the finder
# could not size. PREFER one sized [[jump_table]] over the 3
# [[extra_func]] below: find the abs32 table base (the
# `ldr rT,[pc,#..]; add rT,index<<2; ldr/mov pc` dispatcher) and
# add `[[jump_table]] addr=<base> stride=4 count=<CMP bound> format="abs32" entries_mode="auto"`.

[[extra_func]]
addr = 0x080AC2F4
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x167; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x080AC362
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x105; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x080AC36E
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x102; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x080F1246
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x214"

# ── R6 JUMP-TABLE (unsized, admitted as roots): 5 consecutive thumb misses in [0x080F2F32, 0x080F2F64] ──
# Likely the case targets of a computed-jump switch the finder
# could not size. PREFER one sized [[jump_table]] over the 5
# [[extra_func]] below: find the abs32 table base (the
# `ldr rT,[pc,#..]; add rT,index<<2; ldr/mov pc` dispatcher) and
# add `[[jump_table]] addr=<base> stride=4 count=<CMP bound> format="abs32" entries_mode="auto"`.

[[extra_func]]
addr = 0x080F2F32
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x4; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x080F2F3E
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x4; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x080F2F46
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x4; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x080F2F58
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x4; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x080F2F64
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x4; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

# ── R6 JUMP-TABLE (unsized, admitted as roots): 4 consecutive thumb misses in [0x080FE6EE, 0x080FE720] ──
# Likely the case targets of a computed-jump switch the finder
# could not size. PREFER one sized [[jump_table]] over the 4
# [[extra_func]] below: find the abs32 table base (the
# `ldr rT,[pc,#..]; add rT,index<<2; ldr/mov pc` dispatcher) and
# add `[[jump_table]] addr=<base> stride=4 count=<CMP bound> format="abs32" entries_mode="auto"`.

[[extra_func]]
addr = 0x080FE6EE
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x1; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x080FE702
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x5; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x080FE70A
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x3; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x080FE720
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x3; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

# ── R6 JUMP-TABLE (unsized, admitted as roots): 7 consecutive thumb misses in [0x080FE854, 0x080FE8FA] ──
# Likely the case targets of a computed-jump switch the finder
# could not size. PREFER one sized [[jump_table]] over the 7
# [[extra_func]] below: find the abs32 table base (the
# `ldr rT,[pc,#..]; add rT,index<<2; ldr/mov pc` dispatcher) and
# add `[[jump_table]] addr=<base> stride=4 count=<CMP bound> format="abs32" entries_mode="auto"`.

[[extra_func]]
addr = 0x080FE854
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x32; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x080FE85C
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x1; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x080FE894
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x1; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x080FE8A4
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x33; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x080FE8C6
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x1; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x080FE8D6
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x1; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x080FE8FA
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x1; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x081335AC
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x1"

[[extra_func]]
addr = 0x08133B58
mode = "thumb"
note = "R6: proposed from self-heal miss-log; bridged x1"

# Round 7: second demo-campaign sweep, 75 new (skipped 0 dupes; held 0x03007AF0, 0x03007B44).

[[extra_func]]
addr = 0x03006A72
mode = "thumb"
note = "R7: proposed from self-heal miss-log; bridged x21"

[[extra_func]]
addr = 0x08002066
mode = "thumb"
note = "R7: proposed from self-heal miss-log; bridged x109"

[[extra_func]]
addr = 0x080032E8
mode = "thumb"
note = "R7: proposed from self-heal miss-log; bridged x4"

# ── R7 JUMP-TABLE (unsized, admitted as roots): 3 consecutive thumb misses in [0x080046B4, 0x0800470C] ──
# Likely the case targets of a computed-jump switch the finder
# could not size. PREFER one sized [[jump_table]] over the 3
# [[extra_func]] below: find the abs32 table base (the
# `ldr rT,[pc,#..]; add rT,index<<2; ldr/mov pc` dispatcher) and
# add `[[jump_table]] addr=<base> stride=4 count=<CMP bound> format="abs32" entries_mode="auto"`.

[[extra_func]]
addr = 0x080046B4
mode = "thumb"
note = "R7: proposed from self-heal miss-log; bridged x306; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x080046F0
mode = "thumb"
note = "R7: proposed from self-heal miss-log; bridged x1; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x0800470C
mode = "thumb"
note = "R7: proposed from self-heal miss-log; bridged x4; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08004810
mode = "thumb"
note = "R7: proposed from self-heal miss-log; bridged x272"

# ── R7 JUMP-TABLE (unsized, admitted as roots): 24 consecutive thumb misses in [0x0800499C, 0x08004A1A] ──
# Likely the case targets of a computed-jump switch the finder
# could not size. PREFER one sized [[jump_table]] over the 24
# [[extra_func]] below: find the abs32 table base (the
# `ldr rT,[pc,#..]; add rT,index<<2; ldr/mov pc` dispatcher) and
# add `[[jump_table]] addr=<base> stride=4 count=<CMP bound> format="abs32" entries_mode="auto"`.

[[extra_func]]
addr = 0x0800499C
mode = "thumb"
note = "R7: proposed from self-heal miss-log; bridged x173; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x080049A2
mode = "thumb"
note = "R7: proposed from self-heal miss-log; bridged x173; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x080049A6
mode = "thumb"
note = "R7: proposed from self-heal miss-log; bridged x173; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x080049AA
mode = "thumb"
note = "R7: proposed from self-heal miss-log; bridged x173; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x080049AE
mode = "thumb"
note = "R7: proposed from self-heal miss-log; bridged x173; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x080049B2
mode = "thumb"
note = "R7: proposed from self-heal miss-log; bridged x173; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x080049B6
mode = "thumb"
note = "R7: proposed from self-heal miss-log; bridged x173; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x080049BA
mode = "thumb"
note = "R7: proposed from self-heal miss-log; bridged x173; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x080049BE
mode = "thumb"
note = "R7: proposed from self-heal miss-log; bridged x173; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x080049C2
mode = "thumb"
note = "R7: proposed from self-heal miss-log; bridged x173; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x080049C6
mode = "thumb"
note = "R7: proposed from self-heal miss-log; bridged x173; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x080049CA
mode = "thumb"
note = "R7: proposed from self-heal miss-log; bridged x173; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x080049EC
mode = "thumb"
note = "R7: proposed from self-heal miss-log; bridged x64; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x080049F2
mode = "thumb"
note = "R7: proposed from self-heal miss-log; bridged x64; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x080049F6
mode = "thumb"
note = "R7: proposed from self-heal miss-log; bridged x64; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x080049FA
mode = "thumb"
note = "R7: proposed from self-heal miss-log; bridged x64; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x080049FE
mode = "thumb"
note = "R7: proposed from self-heal miss-log; bridged x64; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08004A02
mode = "thumb"
note = "R7: proposed from self-heal miss-log; bridged x64; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08004A06
mode = "thumb"
note = "R7: proposed from self-heal miss-log; bridged x64; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08004A0A
mode = "thumb"
note = "R7: proposed from self-heal miss-log; bridged x64; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08004A0E
mode = "thumb"
note = "R7: proposed from self-heal miss-log; bridged x64; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08004A12
mode = "thumb"
note = "R7: proposed from self-heal miss-log; bridged x64; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08004A16
mode = "thumb"
note = "R7: proposed from self-heal miss-log; bridged x64; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08004A1A
mode = "thumb"
note = "R7: proposed from self-heal miss-log; bridged x64; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08022A2C
mode = "thumb"
note = "R7: proposed from self-heal miss-log; bridged x37"

[[extra_func]]
addr = 0x08022A30
mode = "thumb"
note = "R7: proposed from self-heal miss-log; bridged x235"

[[extra_func]]
addr = 0x08022DCC
mode = "thumb"
note = "R7: proposed from self-heal miss-log; bridged x4"

[[extra_func]]
addr = 0x080230AC
mode = "thumb"
note = "R7: proposed from self-heal miss-log; bridged x1"

[[extra_func]]
addr = 0x08026898
mode = "thumb"
note = "R7: proposed from self-heal miss-log; bridged x1"

# ── R7 JUMP-TABLE (unsized, admitted as roots): 3 consecutive thumb misses in [0x08027280, 0x080272EC] ──
# Likely the case targets of a computed-jump switch the finder
# could not size. PREFER one sized [[jump_table]] over the 3
# [[extra_func]] below: find the abs32 table base (the
# `ldr rT,[pc,#..]; add rT,index<<2; ldr/mov pc` dispatcher) and
# add `[[jump_table]] addr=<base> stride=4 count=<CMP bound> format="abs32" entries_mode="auto"`.

[[extra_func]]
addr = 0x08027280
mode = "thumb"
note = "R7: proposed from self-heal miss-log; bridged x28; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08027294
mode = "thumb"
note = "R7: proposed from self-heal miss-log; bridged x8; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x080272EC
mode = "thumb"
note = "R7: proposed from self-heal miss-log; bridged x28; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08028702
mode = "thumb"
note = "R7: proposed from self-heal miss-log; bridged x23"

[[extra_func]]
addr = 0x080293CE
mode = "thumb"
note = "R7: proposed from self-heal miss-log; bridged x1"

[[extra_func]]
addr = 0x08029498
mode = "thumb"
note = "R7: proposed from self-heal miss-log; bridged x134"

[[extra_func]]
addr = 0x080294CC
mode = "thumb"
note = "R7: proposed from self-heal miss-log; bridged x135"

# ── R7 JUMP-TABLE (unsized, admitted as roots): 5 consecutive thumb misses in [0x0802B82C, 0x0802B876] ──
# Likely the case targets of a computed-jump switch the finder
# could not size. PREFER one sized [[jump_table]] over the 5
# [[extra_func]] below: find the abs32 table base (the
# `ldr rT,[pc,#..]; add rT,index<<2; ldr/mov pc` dispatcher) and
# add `[[jump_table]] addr=<base> stride=4 count=<CMP bound> format="abs32" entries_mode="auto"`.

[[extra_func]]
addr = 0x0802B82C
mode = "thumb"
note = "R7: proposed from self-heal miss-log; bridged x205; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x0802B832
mode = "thumb"
note = "R7: proposed from self-heal miss-log; bridged x237; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x0802B836
mode = "thumb"
note = "R7: proposed from self-heal miss-log; bridged x237; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x0802B85E
mode = "thumb"
note = "R7: proposed from self-heal miss-log; bridged x237; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x0802B876
mode = "thumb"
note = "R7: proposed from self-heal miss-log; bridged x4; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

# ── R7 JUMP-TABLE (unsized, admitted as roots): 4 consecutive thumb misses in [0x0802BE24, 0x0802BED4] ──
# Likely the case targets of a computed-jump switch the finder
# could not size. PREFER one sized [[jump_table]] over the 4
# [[extra_func]] below: find the abs32 table base (the
# `ldr rT,[pc,#..]; add rT,index<<2; ldr/mov pc` dispatcher) and
# add `[[jump_table]] addr=<base> stride=4 count=<CMP bound> format="abs32" entries_mode="auto"`.

[[extra_func]]
addr = 0x0802BE24
mode = "thumb"
note = "R7: proposed from self-heal miss-log; bridged x1624; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x0802BE5C
mode = "thumb"
note = "R7: proposed from self-heal miss-log; bridged x56; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x0802BE68
mode = "thumb"
note = "R7: proposed from self-heal miss-log; bridged x56; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x0802BED4
mode = "thumb"
note = "R7: proposed from self-heal miss-log; bridged x256; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x0802BF98
mode = "thumb"
note = "R7: proposed from self-heal miss-log; bridged x1368"

# ── R7 JUMP-TABLE (unsized, admitted as roots): 8 consecutive thumb misses in [0x0802C02C, 0x0802C180] ──
# Likely the case targets of a computed-jump switch the finder
# could not size. PREFER one sized [[jump_table]] over the 8
# [[extra_func]] below: find the abs32 table base (the
# `ldr rT,[pc,#..]; add rT,index<<2; ldr/mov pc` dispatcher) and
# add `[[jump_table]] addr=<base> stride=4 count=<CMP bound> format="abs32" entries_mode="auto"`.

[[extra_func]]
addr = 0x0802C02C
mode = "thumb"
note = "R7: proposed from self-heal miss-log; bridged x812; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x0802C064
mode = "thumb"
note = "R7: proposed from self-heal miss-log; bridged x28; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x0802C070
mode = "thumb"
note = "R7: proposed from self-heal miss-log; bridged x28; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x0802C0A6
mode = "thumb"
note = "R7: proposed from self-heal miss-log; bridged x12; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x0802C0E8
mode = "thumb"
note = "R7: proposed from self-heal miss-log; bridged x192; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x0802C158
mode = "thumb"
note = "R7: proposed from self-heal miss-log; bridged x16; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x0802C17C
mode = "thumb"
note = "R7: proposed from self-heal miss-log; bridged x620; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x0802C180
mode = "thumb"
note = "R7: proposed from self-heal miss-log; bridged x620; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

# ── R7 JUMP-TABLE (unsized, admitted as roots): 3 consecutive thumb misses in [0x08034034, 0x08034076] ──
# Likely the case targets of a computed-jump switch the finder
# could not size. PREFER one sized [[jump_table]] over the 3
# [[extra_func]] below: find the abs32 table base (the
# `ldr rT,[pc,#..]; add rT,index<<2; ldr/mov pc` dispatcher) and
# add `[[jump_table]] addr=<base> stride=4 count=<CMP bound> format="abs32" entries_mode="auto"`.

[[extra_func]]
addr = 0x08034034
mode = "thumb"
note = "R7: proposed from self-heal miss-log; bridged x187; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08034048
mode = "thumb"
note = "R7: proposed from self-heal miss-log; bridged x187; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08034076
mode = "thumb"
note = "R7: proposed from self-heal miss-log; bridged x4; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x0803415E
mode = "thumb"
note = "R7: proposed from self-heal miss-log; bridged x48"

[[extra_func]]
addr = 0x0805EA1C
mode = "thumb"
note = "R7: proposed from self-heal miss-log; bridged x1"

[[extra_func]]
addr = 0x0805EA20
mode = "thumb"
note = "R7: proposed from self-heal miss-log; bridged x4"

[[extra_func]]
addr = 0x0805EAB8
mode = "thumb"
note = "R7: proposed from self-heal miss-log; bridged x4"

[[extra_func]]
addr = 0x0805EDBC
mode = "thumb"
note = "R7: proposed from self-heal miss-log; bridged x1"

[[extra_func]]
addr = 0x080612FC
mode = "thumb"
note = "R7: proposed from self-heal miss-log; bridged x4"

[[extra_func]]
addr = 0x08061354
mode = "thumb"
note = "R7: proposed from self-heal miss-log; bridged x103"

[[extra_func]]
addr = 0x080ABCDC
mode = "thumb"
note = "R7: proposed from self-heal miss-log; bridged x272"

[[extra_func]]
addr = 0x080F0FA0
mode = "thumb"
note = "R7: proposed from self-heal miss-log; bridged x272"

[[extra_func]]
addr = 0x080F2EAC
mode = "thumb"
note = "R7: proposed from self-heal miss-log; bridged x4"

[[extra_func]]
addr = 0x080FE678
mode = "thumb"
note = "R7: proposed from self-heal miss-log; bridged x169"

# Round 8: demo-3000 sweep, 69 new (skipped 0 dupes; held 0x03007AF0, 0x03007B44).

# ── R8 JUMP-TABLE (unsized, admitted as roots): 6 consecutive thumb misses in [0x03005FF2, 0x03006052] ──
# Likely the case targets of a computed-jump switch the finder
# could not size. PREFER one sized [[jump_table]] over the 6
# [[extra_func]] below: find the abs32 table base (the
# `ldr rT,[pc,#..]; add rT,index<<2; ldr/mov pc` dispatcher) and
# add `[[jump_table]] addr=<base> stride=4 count=<CMP bound> format="abs32" entries_mode="auto"`.

[[extra_func]]
addr = 0x03005FF2
mode = "thumb"
note = "R8: proposed from self-heal miss-log; bridged x3; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x03005FFC
mode = "thumb"
note = "R8: proposed from self-heal miss-log; bridged x1; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x03006046
mode = "thumb"
note = "R8: proposed from self-heal miss-log; bridged x1; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x03006048
mode = "thumb"
note = "R8: proposed from self-heal miss-log; bridged x1; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x0300604E
mode = "thumb"
note = "R8: proposed from self-heal miss-log; bridged x1; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x03006052
mode = "thumb"
note = "R8: proposed from self-heal miss-log; bridged x1; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x03006982
mode = "thumb"
note = "R8: proposed from self-heal miss-log; bridged x1"

[[extra_func]]
addr = 0x03006A0C
mode = "thumb"
note = "R8: proposed from self-heal miss-log; bridged x1"

[[extra_func]]
addr = 0x08000764
mode = "thumb"
note = "R8: proposed from self-heal miss-log; bridged x16"

[[extra_func]]
addr = 0x080026C4
mode = "thumb"
note = "R8: proposed from self-heal miss-log; bridged x12"

[[extra_func]]
addr = 0x080026E0
mode = "thumb"
note = "R8: proposed from self-heal miss-log; bridged x14"

[[extra_func]]
addr = 0x080028CC
mode = "thumb"
note = "R8: proposed from self-heal miss-log; bridged x14"

[[extra_func]]
addr = 0x08003FE6
mode = "thumb"
note = "R8: proposed from self-heal miss-log; bridged x6"

[[extra_func]]
addr = 0x08003FF4
mode = "thumb"
note = "R8: proposed from self-heal miss-log; bridged x11"

# ── R8 JUMP-TABLE (unsized, admitted as roots): 3 consecutive thumb misses in [0x08022936, 0x08022980] ──
# Likely the case targets of a computed-jump switch the finder
# could not size. PREFER one sized [[jump_table]] over the 3
# [[extra_func]] below: find the abs32 table base (the
# `ldr rT,[pc,#..]; add rT,index<<2; ldr/mov pc` dispatcher) and
# add `[[jump_table]] addr=<base> stride=4 count=<CMP bound> format="abs32" entries_mode="auto"`.

[[extra_func]]
addr = 0x08022936
mode = "thumb"
note = "R8: proposed from self-heal miss-log; bridged x329; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x0802293A
mode = "thumb"
note = "R8: proposed from self-heal miss-log; bridged x40; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08022980
mode = "thumb"
note = "R8: proposed from self-heal miss-log; bridged x2; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08022BA4
mode = "thumb"
note = "R8: proposed from self-heal miss-log; bridged x2"

# ── R8 JUMP-TABLE (unsized, admitted as roots): 14 consecutive thumb misses in [0x08022D1C, 0x08022F0E] ──
# Likely the case targets of a computed-jump switch the finder
# could not size. PREFER one sized [[jump_table]] over the 14
# [[extra_func]] below: find the abs32 table base (the
# `ldr rT,[pc,#..]; add rT,index<<2; ldr/mov pc` dispatcher) and
# add `[[jump_table]] addr=<base> stride=4 count=<CMP bound> format="abs32" entries_mode="auto"`.

[[extra_func]]
addr = 0x08022D1C
mode = "thumb"
note = "R8: proposed from self-heal miss-log; bridged x5; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08022D92
mode = "thumb"
note = "R8: proposed from self-heal miss-log; bridged x1; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08022DA6
mode = "thumb"
note = "R8: proposed from self-heal miss-log; bridged x1; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08022DAE
mode = "thumb"
note = "R8: proposed from self-heal miss-log; bridged x1; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08022DB0
mode = "thumb"
note = "R8: proposed from self-heal miss-log; bridged x2; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08022DBA
mode = "thumb"
note = "R8: proposed from self-heal miss-log; bridged x1; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08022DBC
mode = "thumb"
note = "R8: proposed from self-heal miss-log; bridged x1; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08022DC0
mode = "thumb"
note = "R8: proposed from self-heal miss-log; bridged x1; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08022DCE
mode = "thumb"
note = "R8: proposed from self-heal miss-log; bridged x1; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08022DD4
mode = "thumb"
note = "R8: proposed from self-heal miss-log; bridged x1; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08022E2C
mode = "thumb"
note = "R8: proposed from self-heal miss-log; bridged x1; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08022E88
mode = "thumb"
note = "R8: proposed from self-heal miss-log; bridged x1; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08022EE4
mode = "thumb"
note = "R8: proposed from self-heal miss-log; bridged x2; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08022F0E
mode = "thumb"
note = "R8: proposed from self-heal miss-log; bridged x1; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

# ── R8 JUMP-TABLE (unsized, admitted as roots): 3 consecutive thumb misses in [0x08022FD8, 0x08022FF6] ──
# Likely the case targets of a computed-jump switch the finder
# could not size. PREFER one sized [[jump_table]] over the 3
# [[extra_func]] below: find the abs32 table base (the
# `ldr rT,[pc,#..]; add rT,index<<2; ldr/mov pc` dispatcher) and
# add `[[jump_table]] addr=<base> stride=4 count=<CMP bound> format="abs32" entries_mode="auto"`.

[[extra_func]]
addr = 0x08022FD8
mode = "thumb"
note = "R8: proposed from self-heal miss-log; bridged x96; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08022FDC
mode = "thumb"
note = "R8: proposed from self-heal miss-log; bridged x4000; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08022FF6
mode = "thumb"
note = "R8: proposed from self-heal miss-log; bridged x2; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x0802725E
mode = "thumb"
note = "R8: proposed from self-heal miss-log; bridged x9"

[[extra_func]]
addr = 0x0802A464
mode = "thumb"
note = "R8: proposed from self-heal miss-log; bridged x98"

[[extra_func]]
addr = 0x0802B860
mode = "thumb"
note = "R8: proposed from self-heal miss-log; bridged x80"

[[extra_func]]
addr = 0x0802B880
mode = "thumb"
note = "R8: proposed from self-heal miss-log; bridged x665"

[[extra_func]]
addr = 0x0802B930
mode = "thumb"
note = "R8: proposed from self-heal miss-log; bridged x75"

[[extra_func]]
addr = 0x0802B97C
mode = "thumb"
note = "R8: proposed from self-heal miss-log; bridged x272"

[[extra_func]]
addr = 0x08032E10
mode = "thumb"
note = "R8: proposed from self-heal miss-log; bridged x910"

[[extra_func]]
addr = 0x0803309E
mode = "thumb"
note = "R8: proposed from self-heal miss-log; bridged x1"

[[extra_func]]
addr = 0x08034070
mode = "thumb"
note = "R8: proposed from self-heal miss-log; bridged x16"

[[extra_func]]
addr = 0x0803412C
mode = "thumb"
note = "R8: proposed from self-heal miss-log; bridged x638"

[[extra_func]]
addr = 0x08034158
mode = "thumb"
note = "R8: proposed from self-heal miss-log; bridged x256"

# ── R8 JUMP-TABLE (unsized, admitted as roots): 3 consecutive thumb misses in [0x08061306, 0x0806130E] ──
# Likely the case targets of a computed-jump switch the finder
# could not size. PREFER one sized [[jump_table]] over the 3
# [[extra_func]] below: find the abs32 table base (the
# `ldr rT,[pc,#..]; add rT,index<<2; ldr/mov pc` dispatcher) and
# add `[[jump_table]] addr=<base> stride=4 count=<CMP bound> format="abs32" entries_mode="auto"`.

[[extra_func]]
addr = 0x08061306
mode = "thumb"
note = "R8: proposed from self-heal miss-log; bridged x1; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x0806130A
mode = "thumb"
note = "R8: proposed from self-heal miss-log; bridged x16; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x0806130E
mode = "thumb"
note = "R8: proposed from self-heal miss-log; bridged x1; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x0806143C
mode = "thumb"
note = "R8: proposed from self-heal miss-log; bridged x200"

[[extra_func]]
addr = 0x080ABD6A
mode = "thumb"
note = "R8: proposed from self-heal miss-log; bridged x8"

[[extra_func]]
addr = 0x080ABD72
mode = "thumb"
note = "R8: proposed from self-heal miss-log; bridged x9"

[[extra_func]]
addr = 0x080ABF08
mode = "thumb"
note = "R8: proposed from self-heal miss-log; bridged x172"

[[extra_func]]
addr = 0x080ABF92
mode = "thumb"
note = "R8: proposed from self-heal miss-log; bridged x13"

[[extra_func]]
addr = 0x080AC29C
mode = "thumb"
note = "R8: proposed from self-heal miss-log; bridged x167"

[[extra_func]]
addr = 0x080AC354
mode = "thumb"
note = "R8: proposed from self-heal miss-log; bridged x13"

[[extra_func]]
addr = 0x080AC8B8
mode = "thumb"
note = "R8: proposed from self-heal miss-log; bridged x1"

[[extra_func]]
addr = 0x080F0FC8
mode = "thumb"
note = "R8: proposed from self-heal miss-log; bridged x1"

[[extra_func]]
addr = 0x080F1016
mode = "thumb"
note = "R8: proposed from self-heal miss-log; bridged x368"

[[extra_func]]
addr = 0x080F17A6
mode = "thumb"
note = "R8: proposed from self-heal miss-log; bridged x1"

[[extra_func]]
addr = 0x080F188C
mode = "thumb"
note = "R8: proposed from self-heal miss-log; bridged x360"

[[extra_func]]
addr = 0x080F1894
mode = "thumb"
note = "R8: proposed from self-heal miss-log; bridged x8"

[[extra_func]]
addr = 0x080F2EE8
mode = "thumb"
note = "R8: proposed from self-heal miss-log; bridged x11"

[[extra_func]]
addr = 0x080F2EEE
mode = "thumb"
note = "R8: proposed from self-heal miss-log; bridged x6"

# ── R8 JUMP-TABLE (unsized, admitted as roots): 3 consecutive thumb misses in [0x080FE6B0, 0x080FE6FE] ──
# Likely the case targets of a computed-jump switch the finder
# could not size. PREFER one sized [[jump_table]] over the 3
# [[extra_func]] below: find the abs32 table base (the
# `ldr rT,[pc,#..]; add rT,index<<2; ldr/mov pc` dispatcher) and
# add `[[jump_table]] addr=<base> stride=4 count=<CMP bound> format="abs32" entries_mode="auto"`.

[[extra_func]]
addr = 0x080FE6B0
mode = "thumb"
note = "R8: proposed from self-heal miss-log; bridged x1; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x080FE6C6
mode = "thumb"
note = "R8: proposed from self-heal miss-log; bridged x98; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x080FE6FE
mode = "thumb"
note = "R8: proposed from self-heal miss-log; bridged x5; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x080FE844
mode = "thumb"
note = "R8: proposed from self-heal miss-log; bridged x32"

[[extra_func]]
addr = 0x080FE8A0
mode = "thumb"
note = "R8: proposed from self-heal miss-log; bridged x33"

# Round 9: demo-2000 sweep, 30 new (skipped 0 dupes; held 0x03007AF0, 0x03007B44).

[[extra_func]]
addr = 0x080032A6
mode = "thumb"
note = "R9: proposed from self-heal miss-log; bridged x1"

[[extra_func]]
addr = 0x080032AE
mode = "thumb"
note = "R9: proposed from self-heal miss-log; bridged x9"

[[extra_func]]
addr = 0x08022A40
mode = "thumb"
note = "R9: proposed from self-heal miss-log; bridged x313"

[[extra_func]]
addr = 0x08029ED4
mode = "thumb"
note = "R9: proposed from self-heal miss-log; bridged x1"

[[extra_func]]
addr = 0x08029F50
mode = "thumb"
note = "R9: proposed from self-heal miss-log; bridged x169"

[[extra_func]]
addr = 0x0802ADDC
mode = "thumb"
note = "R9: proposed from self-heal miss-log; bridged x1"

[[extra_func]]
addr = 0x0802BE38
mode = "thumb"
note = "R9: proposed from self-heal miss-log; bridged x495"

[[extra_func]]
addr = 0x0802BE50
mode = "thumb"
note = "R9: proposed from self-heal miss-log; bridged x18"

[[extra_func]]
addr = 0x0802BF10
mode = "thumb"
note = "R9: proposed from self-heal miss-log; bridged x9"

[[extra_func]]
addr = 0x0802BF4C
mode = "thumb"
note = "R9: proposed from self-heal miss-log; bridged x9"

[[extra_func]]
addr = 0x08061458
mode = "thumb"
note = "R9: proposed from self-heal miss-log; bridged x144"

# ── R9 JUMP-TABLE (unsized, admitted as roots): 5 consecutive thumb misses in [0x080ABCE8, 0x080ABD3C] ──
# Likely the case targets of a computed-jump switch the finder
# could not size. PREFER one sized [[jump_table]] over the 5
# [[extra_func]] below: find the abs32 table base (the
# `ldr rT,[pc,#..]; add rT,index<<2; ldr/mov pc` dispatcher) and
# add `[[jump_table]] addr=<base> stride=4 count=<CMP bound> format="abs32" entries_mode="auto"`.

[[extra_func]]
addr = 0x080ABCE8
mode = "thumb"
note = "R9: proposed from self-heal miss-log; bridged x3; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x080ABCF6
mode = "thumb"
note = "R9: proposed from self-heal miss-log; bridged x4; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x080ABCFA
mode = "thumb"
note = "R9: proposed from self-heal miss-log; bridged x5; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x080ABD38
mode = "thumb"
note = "R9: proposed from self-heal miss-log; bridged x5; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x080ABD3C
mode = "thumb"
note = "R9: proposed from self-heal miss-log; bridged x6; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x080ABE54
mode = "thumb"
note = "R9: proposed from self-heal miss-log; bridged x243"

[[extra_func]]
addr = 0x080ABE58
mode = "thumb"
note = "R9: proposed from self-heal miss-log; bridged x57"

[[extra_func]]
addr = 0x080AC02C
mode = "thumb"
note = "R9: proposed from self-heal miss-log; bridged x3"

[[extra_func]]
addr = 0x080AC06C
mode = "thumb"
note = "R9: proposed from self-heal miss-log; bridged x10"

[[extra_func]]
addr = 0x080AC494
mode = "thumb"
note = "R9: proposed from self-heal miss-log; bridged x57"

[[extra_func]]
addr = 0x080AC754
mode = "thumb"
note = "R9: proposed from self-heal miss-log; bridged x130"

[[extra_func]]
addr = 0x080F0FAC
mode = "thumb"
note = "R9: proposed from self-heal miss-log; bridged x1"

[[extra_func]]
addr = 0x080F10EC
mode = "thumb"
note = "R9: proposed from self-heal miss-log; bridged x296"

[[extra_func]]
addr = 0x080F10FA
mode = "thumb"
note = "R9: proposed from self-heal miss-log; bridged x17"

[[extra_func]]
addr = 0x080F14A0
mode = "thumb"
note = "R9: proposed from self-heal miss-log; bridged x312"

[[extra_func]]
addr = 0x080F152A
mode = "thumb"
note = "R9: proposed from self-heal miss-log; bridged x1"

[[extra_func]]
addr = 0x080F17B2
mode = "thumb"
note = "R9: proposed from self-heal miss-log; bridged x17"

[[extra_func]]
addr = 0x080F18C4
mode = "thumb"
note = "R9: proposed from self-heal miss-log; bridged x144"

[[extra_func]]
addr = 0x080F2EC2
mode = "thumb"
note = "R9: proposed from self-heal miss-log; bridged x9"

# Round 10: strict-static abort evidence (boot_strict300, first miss
# pc=0x08001E9A thumb). Bytes BD00 (POP {PC}) directly after BX R0 — same
# PLT-trampoline tail shape as admitted twin 0x08001E8E. Direct-call target.

[[extra_func]]
addr = 0x08001E9A
mode = "thumb"
note = "R10: strict abort; POP{PC} stub twin of 0x08001E8E"

[[extra_func]]
addr = 0x0802B370
mode = "thumb"
note = "R10: strict abort; PUSH-prologue function after literal pool"

# Round 12: live play session (265 misses incl. 0x803Dxxx/0x80470xx/0x80AC0xx exploration), 263 new (skipped 0 dupes; held 0x03007AF0, 0x03007B44).

[[extra_func]]
addr = 0x030072AA
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x36"

[[extra_func]]
addr = 0x03007338
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x1"

[[extra_func]]
addr = 0x0300742E
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x1"

# ── R12 JUMP-TABLE (unsized, admitted as roots): 3 consecutive thumb misses in [0x080017EC, 0x0800180A] ──
# Likely the case targets of a computed-jump switch the finder
# could not size. PREFER one sized [[jump_table]] over the 3
# [[extra_func]] below: find the abs32 table base (the
# `ldr rT,[pc,#..]; add rT,index<<2; ldr/mov pc` dispatcher) and
# add `[[jump_table]] addr=<base> stride=4 count=<CMP bound> format="abs32" entries_mode="auto"`.

[[extra_func]]
addr = 0x080017EC
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x1; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x080017FA
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x1; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x0800180A
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x1; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x080018AE
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x1"

[[extra_func]]
addr = 0x080018C4
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x1"

[[extra_func]]
addr = 0x08001A48
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x1"

[[extra_func]]
addr = 0x08001DD6
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x2"

[[extra_func]]
addr = 0x08002E42
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x10"

[[extra_func]]
addr = 0x08026B9A
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x3"

[[extra_func]]
addr = 0x08026BE2
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x3"

[[extra_func]]
addr = 0x08026EE0
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x34"

[[extra_func]]
addr = 0x08026FA4
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x3"

# ── R12 JUMP-TABLE (unsized, admitted as roots): 15 consecutive thumb misses in [0x08027040, 0x08027192] ──
# Likely the case targets of a computed-jump switch the finder
# could not size. PREFER one sized [[jump_table]] over the 15
# [[extra_func]] below: find the abs32 table base (the
# `ldr rT,[pc,#..]; add rT,index<<2; ldr/mov pc` dispatcher) and
# add `[[jump_table]] addr=<base> stride=4 count=<CMP bound> format="abs32" entries_mode="auto"`.

[[extra_func]]
addr = 0x08027040
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x5; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08027056
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x2; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08027088
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x6; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x0802709C
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x3; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x080270A0
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x3; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x080270A4
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x3; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x080270A8
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x3; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x080270AA
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x1361; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x080270C4
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x6; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x080270D4
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x3; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08027122
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x38; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08027154
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x35; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08027170
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x2; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08027172
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x34; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08027192
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x21; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x080273A2
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x1"

[[extra_func]]
addr = 0x0802793C
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x3"

[[extra_func]]
addr = 0x0802820A
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x1"

# ── R12 JUMP-TABLE (unsized, admitted as roots): 10 consecutive thumb misses in [0x0802C374, 0x0802C49C] ──
# Likely the case targets of a computed-jump switch the finder
# could not size. PREFER one sized [[jump_table]] over the 10
# [[extra_func]] below: find the abs32 table base (the
# `ldr rT,[pc,#..]; add rT,index<<2; ldr/mov pc` dispatcher) and
# add `[[jump_table]] addr=<base> stride=4 count=<CMP bound> format="abs32" entries_mode="auto"`.

[[extra_func]]
addr = 0x0802C374
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x1230; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x0802C390
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x10; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x0802C3B8
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x10; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x0802C3E0
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x10; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x0802C3E4
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x10; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x0802C414
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x10; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x0802C42A
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x2; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x0802C46E
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x1; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x0802C470
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x1220; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x0802C49C
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x1220; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x0802C5E0
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x1220"

[[extra_func]]
addr = 0x0802C636
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x16"

# ── R12 JUMP-TABLE (unsized, admitted as roots): 7 consecutive thumb misses in [0x0802C70E, 0x0802C7F4] ──
# Likely the case targets of a computed-jump switch the finder
# could not size. PREFER one sized [[jump_table]] over the 7
# [[extra_func]] below: find the abs32 table base (the
# `ldr rT,[pc,#..]; add rT,index<<2; ldr/mov pc` dispatcher) and
# add `[[jump_table]] addr=<base> stride=4 count=<CMP bound> format="abs32" entries_mode="auto"`.

[[extra_func]]
addr = 0x0802C70E
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x1; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x0802C75C
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x42; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x0802C774
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x3; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x0802C7AE
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x3; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x0802C7CE
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x3; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x0802C7E8
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x42; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x0802C7F4
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x31; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

# ── R12 JUMP-TABLE (unsized, admitted as roots): 8 consecutive thumb misses in [0x0802C882, 0x0802C930] ──
# Likely the case targets of a computed-jump switch the finder
# could not size. PREFER one sized [[jump_table]] over the 8
# [[extra_func]] below: find the abs32 table base (the
# `ldr rT,[pc,#..]; add rT,index<<2; ldr/mov pc` dispatcher) and
# add `[[jump_table]] addr=<base> stride=4 count=<CMP bound> format="abs32" entries_mode="auto"`.

[[extra_func]]
addr = 0x0802C882
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x1; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x0802C888
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x2; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x0802C88C
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x2; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x0802C892
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x1; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x0802C896
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x1; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x0802C8C0
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x3; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x0802C90C
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x2; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x0802C930
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x1; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

# ── R12 JUMP-TABLE (unsized, admitted as roots): 5 consecutive thumb misses in [0x0802C9F8, 0x0802CA84] ──
# Likely the case targets of a computed-jump switch the finder
# could not size. PREFER one sized [[jump_table]] over the 5
# [[extra_func]] below: find the abs32 table base (the
# `ldr rT,[pc,#..]; add rT,index<<2; ldr/mov pc` dispatcher) and
# add `[[jump_table]] addr=<base> stride=4 count=<CMP bound> format="abs32" entries_mode="auto"`.

[[extra_func]]
addr = 0x0802C9F8
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x106; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x0802CA0C
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x2; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x0802CA5C
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x2; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x0802CA72
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x2; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x0802CA84
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x106; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x0802DF3C
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x1"

[[extra_func]]
addr = 0x0802DFB2
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x126"

# ── R12 JUMP-TABLE (unsized, admitted as roots): 6 consecutive thumb misses in [0x0802E0C4, 0x0802E1B0] ──
# Likely the case targets of a computed-jump switch the finder
# could not size. PREFER one sized [[jump_table]] over the 6
# [[extra_func]] below: find the abs32 table base (the
# `ldr rT,[pc,#..]; add rT,index<<2; ldr/mov pc` dispatcher) and
# add `[[jump_table]] addr=<base> stride=4 count=<CMP bound> format="abs32" entries_mode="auto"`.

[[extra_func]]
addr = 0x0802E0C4
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x87; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x0802E0D0
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x28; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x0802E100
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x21; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x0802E156
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x19; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x0802E17A
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x5; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x0802E1B0
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x29; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

# ── R12 JUMP-TABLE (unsized, admitted as roots): 27 consecutive thumb misses in [0x0802F7A0, 0x0802FB42] ──
# Likely the case targets of a computed-jump switch the finder
# could not size. PREFER one sized [[jump_table]] over the 27
# [[extra_func]] below: find the abs32 table base (the
# `ldr rT,[pc,#..]; add rT,index<<2; ldr/mov pc` dispatcher) and
# add `[[jump_table]] addr=<base> stride=4 count=<CMP bound> format="abs32" entries_mode="auto"`.

[[extra_func]]
addr = 0x0802F7A0
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x1; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x0802F7D4
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x1; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x0802F804
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x12; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x0802F828
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x5; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x0802F84C
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x3; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x0802F86C
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x4; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x0802F8AC
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x1; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x0802F8EC
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x17; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x0802F924
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x1; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x0802F930
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x1; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x0802F934
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x1; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x0802F938
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x1; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x0802F93C
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x1; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x0802F98C
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x17; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x0802F9B8
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x1; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x0802F9E2
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x3; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x0802F9FC
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x1; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x0802FA04
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x1; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x0802FA10
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x3; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x0802FA58
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x1; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x0802FA68
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x1; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x0802FA72
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x1; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x0802FA88
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x383; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x0802FACA
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x1; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x0802FAD8
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x1; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x0802FAEA
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x1; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x0802FB42
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x383; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x0802FBF8
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x29"

[[extra_func]]
addr = 0x0802FBFC
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x29"

# ── R12 JUMP-TABLE (unsized, admitted as roots): 9 consecutive thumb misses in [0x08031CF0, 0x08031DCE] ──
# Likely the case targets of a computed-jump switch the finder
# could not size. PREFER one sized [[jump_table]] over the 9
# [[extra_func]] below: find the abs32 table base (the
# `ldr rT,[pc,#..]; add rT,index<<2; ldr/mov pc` dispatcher) and
# add `[[jump_table]] addr=<base> stride=4 count=<CMP bound> format="abs32" entries_mode="auto"`.

[[extra_func]]
addr = 0x08031CF0
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x383; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08031D24
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x1; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08031D2C
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x1; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08031D30
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x1; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08031D34
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x1; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08031D4C
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x1; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08031D98
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x383; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08031DA2
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x383; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08031DCE
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x383; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x0803339A
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x31"

[[extra_func]]
addr = 0x08033440
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x30"

# ── R12 JUMP-TABLE (unsized, admitted as roots): 12 consecutive thumb misses in [0x08033B04, 0x08033CDC] ──
# Likely the case targets of a computed-jump switch the finder
# could not size. PREFER one sized [[jump_table]] over the 12
# [[extra_func]] below: find the abs32 table base (the
# `ldr rT,[pc,#..]; add rT,index<<2; ldr/mov pc` dispatcher) and
# add `[[jump_table]] addr=<base> stride=4 count=<CMP bound> format="abs32" entries_mode="auto"`.

[[extra_func]]
addr = 0x08033B04
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x1; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08033B12
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x60; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08033B68
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x1; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08033B72
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x60; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08033BA8
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x1; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08033BC0
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x60; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08033C08
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x60; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08033C44
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x1; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08033C4E
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x60; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08033C90
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x10; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08033C9A
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x51; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08033CDC
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x45; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08034198
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x15"

# ── R12 JUMP-TABLE (unsized, admitted as roots): 23 consecutive thumb misses in [0x0803471C, 0x08034904] ──
# Likely the case targets of a computed-jump switch the finder
# could not size. PREFER one sized [[jump_table]] over the 23
# [[extra_func]] below: find the abs32 table base (the
# `ldr rT,[pc,#..]; add rT,index<<2; ldr/mov pc` dispatcher) and
# add `[[jump_table]] addr=<base> stride=4 count=<CMP bound> format="abs32" entries_mode="auto"`.

[[extra_func]]
addr = 0x0803471C
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x1; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x0803472E
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x1; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08034734
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x1; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x080347B0
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x1; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x080347B8
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x1; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08034800
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x24; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x0803480E
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x24; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x0803482A
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x24; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08034830
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x16; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x0803483C
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x16; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08034842
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x16; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08034846
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x16; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x0803485A
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x16; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08034866
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x1; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x0803486A
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x1; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x0803487C
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x1; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08034896
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x1; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x080348A8
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x30; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x080348C8
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x30; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x080348D4
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x30; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x080348D8
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x30; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x080348F8
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x30; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08034904
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x30; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

# ── R12 JUMP-TABLE (unsized, admitted as roots): 13 consecutive thumb misses in [0x0803499E, 0x08034B6C] ──
# Likely the case targets of a computed-jump switch the finder
# could not size. PREFER one sized [[jump_table]] over the 13
# [[extra_func]] below: find the abs32 table base (the
# `ldr rT,[pc,#..]; add rT,index<<2; ldr/mov pc` dispatcher) and
# add `[[jump_table]] addr=<base> stride=4 count=<CMP bound> format="abs32" entries_mode="auto"`.

[[extra_func]]
addr = 0x0803499E
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x24; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x080349A6
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x24; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x080349AE
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x84; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x080349B2
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x84; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x080349C0
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x22; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08034A14
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x24; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08034A2E
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x24; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08034A64
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x1; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08034A7C
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x1; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08034AB8
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x2; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08034AE6
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x23; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08034B2E
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x2; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08034B6C
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x1; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

# ── R12 JUMP-TABLE (unsized, admitted as roots): 9 consecutive thumb misses in [0x08034EA4, 0x08034F28] ──
# Likely the case targets of a computed-jump switch the finder
# could not size. PREFER one sized [[jump_table]] over the 9
# [[extra_func]] below: find the abs32 table base (the
# `ldr rT,[pc,#..]; add rT,index<<2; ldr/mov pc` dispatcher) and
# add `[[jump_table]] addr=<base> stride=4 count=<CMP bound> format="abs32" entries_mode="auto"`.

[[extra_func]]
addr = 0x08034EA4
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x1; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08034EBA
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x1; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08034ED0
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x1; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08034EDC
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x1; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08034EE6
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x60; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08034EFC
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x1; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08034F12
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x16; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08034F1E
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x31; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08034F28
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x39; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

# ── R12 JUMP-TABLE (unsized, admitted as roots): 5 consecutive thumb misses in [0x0803564C, 0x080356D6] ──
# Likely the case targets of a computed-jump switch the finder
# could not size. PREFER one sized [[jump_table]] over the 5
# [[extra_func]] below: find the abs32 table base (the
# `ldr rT,[pc,#..]; add rT,index<<2; ldr/mov pc` dispatcher) and
# add `[[jump_table]] addr=<base> stride=4 count=<CMP bound> format="abs32" entries_mode="auto"`.

[[extra_func]]
addr = 0x0803564C
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x2; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08035678
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x1; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08035682
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x1; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08035690
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x1; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x080356D6
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x64; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

# ── R12 JUMP-TABLE (unsized, admitted as roots): 14 consecutive thumb misses in [0x08035774, 0x08035948] ──
# Likely the case targets of a computed-jump switch the finder
# could not size. PREFER one sized [[jump_table]] over the 14
# [[extra_func]] below: find the abs32 table base (the
# `ldr rT,[pc,#..]; add rT,index<<2; ldr/mov pc` dispatcher) and
# add `[[jump_table]] addr=<base> stride=4 count=<CMP bound> format="abs32" entries_mode="auto"`.

[[extra_func]]
addr = 0x08035774
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x1; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x0803579C
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x39; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x080357B2
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x39; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x080357BC
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x16; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x080357D0
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x15; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x080357D4
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x53; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08035832
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x45; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08035836
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x1; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x0803584C
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x17; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x080358C4
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x1; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x080358F6
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x124; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x080358FC
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x1; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08035930
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x1; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08035948
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x2; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08035A00
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x108"

[[extra_func]]
addr = 0x08035A5C
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x23"

# ── R12 JUMP-TABLE (unsized, admitted as roots): 13 consecutive thumb misses in [0x08037088, 0x08037248] ──
# Likely the case targets of a computed-jump switch the finder
# could not size. PREFER one sized [[jump_table]] over the 13
# [[extra_func]] below: find the abs32 table base (the
# `ldr rT,[pc,#..]; add rT,index<<2; ldr/mov pc` dispatcher) and
# add `[[jump_table]] addr=<base> stride=4 count=<CMP bound> format="abs32" entries_mode="auto"`.

[[extra_func]]
addr = 0x08037088
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x384; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x080370E2
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x1; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08037130
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x1; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08037138
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x1; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08037168
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x1; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08037196
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x1; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x0803719A
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x1; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x080371B8
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x1; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x080371DA
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x326; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08037202
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x11; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08037236
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x16; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08037244
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x1; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08037248
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x1; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

# ── R12 JUMP-TABLE (unsized, admitted as roots): 42 consecutive thumb misses in [0x080373BA, 0x08037818] ──
# Likely the case targets of a computed-jump switch the finder
# could not size. PREFER one sized [[jump_table]] over the 42
# [[extra_func]] below: find the abs32 table base (the
# `ldr rT,[pc,#..]; add rT,index<<2; ldr/mov pc` dispatcher) and
# add `[[jump_table]] addr=<base> stride=4 count=<CMP bound> format="abs32" entries_mode="auto"`.

[[extra_func]]
addr = 0x080373BA
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x383; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x080373BE
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x383; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x080373F0
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x342; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08037400
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x11; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08037412
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x11; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08037428
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x31; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08037438
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x31; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x0803744A
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x31; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x0803745A
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x31; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x0803746C
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x31; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x0803747E
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x31; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08037490
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x31; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x080374A2
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x31; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x080374B8
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x31; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x080374EA
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x31; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08037502
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x31; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08037508
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x31; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08037522
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x31; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08037528
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x31; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08037542
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x31; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08037548
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x31; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x0803758E
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x31; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x080375C6
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x31; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x080375E4
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x31; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x080375EA
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x31; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08037602
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x31; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08037608
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x31; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08037620
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x31; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08037660
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x124; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08037690
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x31; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x080376AE
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x31; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x080376FA
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x1; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08037704
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x25; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08037756
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x14; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x0803776E
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x1; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08037774
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x1; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x080377A6
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x1; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x080377AC
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x1; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08037802
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x1; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08037808
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x1; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x0803780E
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x1; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08037818
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x321; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

# ── R12 JUMP-TABLE (unsized, admitted as roots): 5 consecutive thumb misses in [0x08037954, 0x080379F4] ──
# Likely the case targets of a computed-jump switch the finder
# could not size. PREFER one sized [[jump_table]] over the 5
# [[extra_func]] below: find the abs32 table base (the
# `ldr rT,[pc,#..]; add rT,index<<2; ldr/mov pc` dispatcher) and
# add `[[jump_table]] addr=<base> stride=4 count=<CMP bound> format="abs32" entries_mode="auto"`.

[[extra_func]]
addr = 0x08037954
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x31; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08037976
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x31; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x08037992
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x31; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x080379E8
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x62; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x080379F4
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x62; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x0803D272
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x1"

# ── R12 JUMP-TABLE (unsized, admitted as roots): 4 consecutive thumb misses in [0x0803D2FE, 0x0803D35C] ──
# Likely the case targets of a computed-jump switch the finder
# could not size. PREFER one sized [[jump_table]] over the 4
# [[extra_func]] below: find the abs32 table base (the
# `ldr rT,[pc,#..]; add rT,index<<2; ldr/mov pc` dispatcher) and
# add `[[jump_table]] addr=<base> stride=4 count=<CMP bound> format="abs32" entries_mode="auto"`.

[[extra_func]]
addr = 0x0803D2FE
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x63; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x0803D308
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x1; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x0803D34C
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x1; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x0803D35C
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x1; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

# ── R12 JUMP-TABLE (unsized, admitted as roots): 3 consecutive thumb misses in [0x0803D540, 0x0803D590] ──
# Likely the case targets of a computed-jump switch the finder
# could not size. PREFER one sized [[jump_table]] over the 3
# [[extra_func]] below: find the abs32 table base (the
# `ldr rT,[pc,#..]; add rT,index<<2; ldr/mov pc` dispatcher) and
# add `[[jump_table]] addr=<base> stride=4 count=<CMP bound> format="abs32" entries_mode="auto"`.

[[extra_func]]
addr = 0x0803D540
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x64; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x0803D568
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x59; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x0803D590
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x113; part of a JUMP-TABLE CANDIDATE (prefer a sized [[jump_table]])"

[[extra_func]]
addr = 0x080470C6
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x1"

[[extra_func]]
addr = 0x080AC0F4
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x39"

[[extra_func]]
addr = 0x080AC1B4
mode = "thumb"
note = "R12: proposed from self-heal miss-log; bridged x3"
